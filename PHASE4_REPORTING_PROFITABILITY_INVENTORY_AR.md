# Phase 4 — التقارير والربحية والمخزون

## الهدف
إضافة مركز تقارير محلي بالكامل فوق SQLite، بحيث تكون نتائج الربحية والمخزون والذمم والصندوق قابلة لإعادة الحساب من المستندات المصدرية، مع دعم الفترات التاريخية و`as-of` دون الاعتماد على الرصيد الحالي فقط.

## المنفذ

### 1. ReportingService
أضيف `src/nano_offline/services/reporting_service.py` ويقدم:

- `income_statement()`
  - المبيعات.
  - تكلفة البضاعة المباعة COGS من `invoice_lines.cost_amount`.
  - مجمل الربح والهامش.
  - المصروفات حسب الفترة والتصنيف.
  - صافي الربح.
  - المشتريات كمعلومة مستقلة، ولا يتم استخدامها بديلًا عن COGS.

- `invoice_profitability()`
  - إجمالي فاتورة البيع.
  - التكلفة.
  - مجمل الربح.
  - هامش الربح.
  - المدفوع والمتبقي.

- `item_profitability()` و`top_selling_items()`
  - الكمية المباعة بالوحدة الأساسية.
  - الإيراد والتكلفة والربح والهامش.
  - عدد فواتير البيع.
  - ترتيب الأكثر مبيعًا.

- `inventory_report()` و`inventory_valuation()`
  - رصيد افتتاحي للفترة.
  - الوارد والصادر.
  - قيمة المشتريات وCOGS الصادر.
  - الرصيد الختامي والقيمة الختامية.
  - متوسط التكلفة الختامي التاريخي.
  - تقييم المخزون كما في تاريخ محدد.

- `party_balances()`
  - ذمم العملاء كما في تاريخ.
  - الأرصدة الدائنة للعملاء.
  - ذمم الموردين.
  - الدفعات المقدمة للموردين.

- `outstanding_invoices()`
  - الفواتير المفتوحة كما في تاريخ محدد.
  - يعتمد على `payment_date` للتوزيعات، لذلك دفعة مستقبلية لا تغير تقريرًا تاريخيًا سابقًا.

- `cash_movement()`
  - الرصيد الافتتاحي.
  - المقبوضات.
  - المدفوعات.
  - صافي الحركة.
  - الرصيد الختامي.
  - رصيد تراكمي لكل حركة.

### 2. مركز التقارير في Flet
أضيف `ReportsCenter` تحت `views/reports_view.py` مع تقارير:

1. قائمة الدخل والربحية.
2. ربحية الفواتير والمواد.
3. حركة وتقييم المخزون.
4. ذمم العملاء والموردين.
5. حركة الصندوق.

الواجهة تستخدم حقلي فترة موحدين وتخطيطًا متجاوبًا بواسطة `ResponsiveRow` وبطاقات مناسبة للهاتف.

### 3. التوافق التاريخي
لا تعتمد تقارير الذمم التاريخية على `customers.balance` أو `suppliers.balance` الحاليين. يتم اشتقاق الرصيد كما في التاريخ من `ledger_entries` المؤرخة.

كذلك، الفواتير المفتوحة التاريخية تعيد حساب ما تم دفعه حتى تاريخ التقرير فقط من `payments.payment_date` + `payment_allocations`.

### 4. المخزون التاريخي
الرصيد الافتتاحي للمادة يعتبر Baseline سابقًا لجميع الحركات، وفق قرار Phase 2. بعدها تستخدم حركات المخزون وقيمها التاريخية لتحديد كمية وقيمة المخزون في أي تاريخ.

## مثال الاختبار المرجعي

- مخزون افتتاحي: 10 وحدات × 10 = 100.
- شراء: 5 × 12 = 60.
- بيع: 6 × 20 = 120.
- COGS للبيع = 64.
- خدمة مباعة = 50، وتكلفتها الحالية في النموذج = 0.
- مصروف = 15.

النتيجة:

- المبيعات = 170.
- COGS = 64.
- مجمل الربح = 106.
- صافي الربح = 91.
- المخزون الختامي = 9 وحدات.
- قيمة المخزون = 96.
- ذمم العملاء الحالية = 120.
- ذمم الموردين = 30.
- رصيد الصندوق = 5.

## ملاحظة حول الخدمات
في الـCore الحالي، الخدمات لا تنتج حركة مخزون ولا `cost_amount` عند البيع، لذلك تظهر تكلفة الخدمة صفرًا ما لم يُضاف لاحقًا نموذج تكلفة خدمات مستقل. لم يتم تغيير هذا السلوك في Phase 4 حتى لا يتغير عقد المراحل السابقة ضمن مرحلة تقارير فقط.

## قاعدة البيانات
لا توجد جداول جديدة في هذه المرحلة؛ لذلك بقي:

`SCHEMA_VERSION = 3`

وتم رفع إصدار التطبيق إلى `0.4.0` و`build_number = 4`.

## الاختبارات

- `phase4_reporting_core_smoke_test.py`
- `phase4_reporting_historical_smoke_test.py`
- `phase4_flet_ui_contract_smoke_test.py`
- `phase4_project_contract_smoke_test.py`
- جميع اختبارات Phase 1–3 بقيت ضمن Quality Gate.

## ما لم ينفذ بعد

- PDF والطباعة والتصدير.
- رسوم بيانية تفاعلية.
- نسخ احتياطي واسترجاع.
- مستخدمون محليون وصلاحيات.
- التفعيل الأونلاين.
- بناء APK فعلي واختبار UI على جهاز.
