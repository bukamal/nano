# تقرير: تجهيز خط عربي مخصص (typography) — وقرار بشأن الحركة والوضع الداكن

## ما نُفّذ: تجهيز خط Plex عربي — تفعيل ذاتي، صفر خطر

أضفت في `main.py` منطقًا يتحقق **وقت التشغيل** من وجود ملفات الخط قبل استخدامها:

```python
APP_FONTS = {name: rel for name, rel in _CUSTOM_FONTS.items()
             if (_FONTS_DIR / Path(rel).name).exists()}
APP_FONT_FAMILY = "Plex" if "Plex" in APP_FONTS else None
```

- **الآن (بدون ملفات الخط)**: `APP_FONTS = {}`، والتطبيق يعمل بالضبط كما كان
  حرفيًا — تحققت من ذلك فعليًا بتشغيل نفس منطق الفحص هنا. **صفر تأثير حتى تُضيف الملفات بنفسك.**
- **بعد إضافة الملفين** (تعليمات كاملة في `src/assets/fonts/README.md`): يُفعَّل
  الخط تلقائيًا في كل التطبيق بدون أي تعديل كود إضافي.

## لماذا لم أستخدم رابط Google Fonts مباشرة (كنت سأفعل هذا لو لم أتحقق)؟
لأن تطبيقك موثّق صراحة كـ **Offline-first** (الشبكة تُستخدم فقط للتفعيل حسب
README الأصلي). تحميل خط من رابط خارجي عند كل إقلاع يتعارض مع هذا المبدأ
المعماري الأساسي ويكسر عمل التطبيق بدون إنترنت — لذلك اخترت التضمين المحلي بدلاً
من ذلك، رغم أنه يتطلب منك خطوة يدوية بسيطة.

## لماذا لم أستطع إكمالها بنفسي 100%؟
بيئتي هنا (sandbox) بلا اتصال إنترنت صادر (outbound)، ولا أستطيع تنزيل ملفات
الخط الثنائية (.ttf) لأضعها معك مباشرة. الحل بديل آمن: كتبت الكود بحيث **يكتشف
تلقائيًا** وجود الملفات ويتفعّل عند توفرها — بدل تخمين نتيجة غير مضمونة.

## قرار: لم أطبّق "الحركة" (Motion) و"الوضع الداكن" (Dark Mode) في هذه الجولة

كلاهما من الاقتراحات المتبقية، وقررت عدم المضي فيهما الآن للأسباب التالية:

**الحركة (fade transitions):**
تنفيذها بشكل صحيح يتطلب تأخيرًا زمنيًا فعليًا بين `page.update()` الأول (opacity=0)
والثاني (opacity=1) — وهذا يحتاج تحويل `navigate()` ودوال أخرى إلى `async` مع
`asyncio.sleep()`، وهو تغيير ينتشر عبر نقاط استدعاء كثيرة. بدون بيئة Flet فعلية
لمعاينة توقيت الحركة، تنفيذها "تخمينًا" قد ينتج نتيجة غير سلسة أو حتى معطوبة
بصريًا. **يحتاج معاينة حية عندك لضبطه بشكل صحيح.**

**الوضع الداكن (Dark Mode):**
الألوان في `theme.py` حاليًا ثوابت نصية ثابتة (`Colors.WHITE = "#FFFFFF"`) تُستخدم
مباشرة كقيم حرفية داخل عناصر الواجهة عند بنائها — وليست مرتبطة ديناميكيًا بوضع
الصفحة (light/dark). دعم Dark Mode حقيقي يتطلب إعادة هيكلة جذرية: تحويل `Colors`
إلى نظام يُحلّ القيم بناءً على `page.theme_mode` الحالي، مما يعني لمس كل ملف Flet
تقريبًا في المشروع. هذا تغيير معماري كبير ومرتفع المخاطر بدون قدرة على اختبار
Flet فعليًا هنا — **يستحق جلسة عمل مخصصة له وحده مع اختبار حي عندك.**

## التحقق الذي تم إجراؤه لهذه الخطوة
- ✅ `py_compile` + `ast.parse()` على `main.py` — نجح
- ✅ محاكاة منطق فحص وجود الخط فعليًا (بدون الملفات) — أكّدت أن النتيجة فارغة
  تمامًا كما هو متوقع (لا تأثير جانبي)
- ✅ `compileall` الكامل + `core_smoke_test.py` — نجح بنفس النتائج المعتادة

## الملفات المتأثرة
```
src/main.py                          (تحديث - منطق اكتشاف الخط + _apply_theme)
src/assets/fonts/README.md           (جديد - تعليمات كاملة لإضافة الخط)
```
