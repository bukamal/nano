# تقرير: إصلاح صوت مسح الباركود في الفاتورة + توحيد صوت الأخطاء + نغمة حذف جديدة

## 1. مسح الباركود داخل الفاتورة كان بلا أي صوت عند النجاح (`invoice_view.py`)

`scan_to_add_line` كانت تضيف البند مباشرة (`add_or_bump_item`) دون أي استدعاء
`notify()` عند النجاح — لا صوت ولا رسالة على الشاشة إطلاقًا، بعكس نقطة البيع
والجرد اللتين تعرضان "✔" بنغمة `scan` مخصصة. الآن `add_or_bump_item` ترجع
`"added"`/`"bumped"` و`scan_to_add_line` تستدعي `notify(..., sound_kind="scan")`
بنفس منطق `pos_settings.scan_feedback_mode` (مختصر/مفصّل) المستخدم في نقطة
البيع، فتصير تجربة المسح متطابقة بين الشاشتين.

رسائل الفشل (باركود غير موجود، الماسح غير مهيأ، استثناء) صارت تُمرّر
`kind="error"` صراحة بدل الاعتماد على تخمين نص الرسالة.

## 2. عشرات نقاط النداء لم تكن تضمن صوت الخطأ الصحيح

`toast()` يستنتج نوع الصوت (نجاح/خطأ/تنبيه/عادي) من كلمات مفتاحية داخل نص
الرسالة نفسها إن لم يُحدَّد `kind` صراحة (انظر `_infer_kind` في `toast.py`).
مشكلة ذلك: `notify(str(exc))` — الاستدعاء الأكثر شيوعًا لعرض استثناء — يمرّر
نص استثناء عشوائي (رسالة قاعدة بيانات، رسالة Python...) قد لا يحوي أيًا من
الكلمات التي يبحث عنها `_infer_kind`، فيسقط افتراضيًا على "عادي" (`info`)
بدل "خطأ" — يعني نغمة خاطئة (أو صامتة أصلًا، بما أن `info` معطّل افتراضيًا).

تم تصحيح أكثر من ٥٠ موضع عبر: `invoice_view.py`, `finance_view.py`,
`items_view.py`, `parties_view.py`, `pos_view.py`, `admin_view.py`,
`reports_view.py`, `security_view.py` — كلها الآن تمرّر `kind="error"` صراحة
عند عرض استثناء. الملفات التي كانت دالة `notify()` فيها لا تقبل `kind`
أصلًا (`finance_view`, `items_view`, `parties_view`, `reports_view`,
`security_view`) أضيف لها الوسيط.

`admin_view._notify_error` (نافذة تنبيه فشل الاستعادة/التحقق) كانت مبنية
كـ `AlertDialog` يدوي لا يمر عبر `toast()` إطلاقًا — أي **بلا أي صوت مطلقًا**
حتى لو كان النظام الصوتي مفعّلاً بالكامل. الآن تستدعي
`sound_engine.play(page, "error")` صراحة قبل فتح النافذة.

## 3. نغمة جديدة: "حذف" (`delete`)

نغمة سابعة (بعد success/error/warning/info/scan/save) لعمليات الحذف
النهائية (فاتورة، مادة، طرف، سند، مصروف، تصنيف/وحدة، نسخة احتياطية) —
نغمتان منخفضتان سريعتا التلاشي (G4 ثم C4)، أخفض حدّة وأقصر مدة من نغمة
الخطأ، حتى لا تُقرأ عملية حذف ناجحة وكأن شيئًا فشل، ومختلفة صوتيًا عن نغمة
النجاح العامة التي كانت تُستنتج تلقائيًا من كلمة "تم" في رسائل الحذف سابقًا.

ربط كامل:
- `src/assets/sounds/delete.wav` +
  `extensions/flet_native_files/.../assets/sounds/delete.wav`.
- `core/sound.py`: أُضيفت لـ`SoundKind` و`_SOUND_KINDS`.
- `core/sound_settings.py`: مفتاح `sound_kind_delete_enabled` (افتراضي: مفعّل).
- `sound_pool.dart`: إدخال جديد في `kSoundAssetPaths`.
- شاشة الإدارة ← تبويب "الصوت": مفتاح تفعيل + زر معاينة "حذف" + سطر في
  لوحة التشخيص.
- ربط فعلي في: حذف فاتورة، حذف مادة، حذف طرف، حذف سند، حذف مصروف،
  حذف تصنيف/وحدة، حذف نسخة احتياطية (شاشة الإدارة).

`tools/sound_system_smoke_test.py` محدَّث ليتحقق من النغمة السابعة في كل
المواضع الأربعة (الإعدادات الافتراضية، ملف WAV بايثون، خريطة Dart، ملف WAV
Flutter) — **الاختبار ناجح**.

## لم يُنفَّذ في هذه الجلسة (بالاتفاق)

- شاشة "التحقق من الفاتورة" عبر مسح QR (`invoice_signing.verify_payload`)
  ما زالت غير مربوطة بأي واجهة — تم تأجيلها بطلب صريح، تركيز هذه الجلسة على
  الصوت والباركود فقط.

## لم يُختبر على جهاز حقيقي

لا توجد بيئة Flutter/جهاز Android هنا للتحقق الفعلي من التشغيل الصوتي على
الجهاز — الفحص هنا مقتصر على: `py_compile` لكل الملفات المعدَّلة، وتشغيل
مجموعة اختبارات المشروع النصية ذات الصلة (`sound_system_smoke_test`,
`stocktake_continuous_scan_smoke_test`, `phase2_invoice_editor_core_smoke_test`,
`phase3_financial_workflow_smoke_test`, `item_delete_guard_smoke_test`,
`definitions_management_smoke_test`) وكلها ناجحة. عدد من اختبارات
`tools/` الأخرى غير المتعلقة بهذا التعديل (فحوصات إصدار مخطط قاعدة
البيانات، نصوص واجهة قديمة، ملفات CI/بناء APK غير موجودة في هذه البيئة)
فاشلة مسبقًا وبشكل غير مرتبط بهذا التعديل إطلاقًا — يُنصح بمراجعتها بشكل
منفصل إن رغبت.
