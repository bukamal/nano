# Nano 0.7.2 — إصلاح واجهة Android والفواتير

هذا الإصدار مخصص لمعالجة مشكلة الشاشة الرمادية في تبويب الفواتير وتحسين تجربة الهاتف.

## ما تم تصحيحه

- إعادة بناء شاشة قائمة الفواتير لتقليل تعقيد الـ widgets على Android.
- إضافة حالة خطأ احتياطية داخل الفواتير بدل ظهور شاشة رمادية صامتة.
- إضافة بحث برقم الفاتورة أو الطرف أو المرجع أو التاريخ.
- إضافة فلترة حسب نوع الفاتورة وحالة السداد.
- إضافة مؤشرات مختصرة: إجمالي المبيعات، المستحق، والفواتير المفتوحة.
- تقليل أزرار كل فاتورة إلى فتح/تسجيل دفعة/المزيد، ونقل الطباعة وPDF والحذف إلى نافذة خيارات.
- تبسيط تبويب المواد: البحث والقائمة أصبحا الواجهة الأساسية، ونموذج الإضافة يظهر عند الطلب.
- نقل التصنيفات والوحدات إلى نافذة مستقلة.
- إضافة SafeArea لمنع تداخل العناوين مع شريط الحالة في Android/iOS.
- رفع الإصدار إلى 0.7.2 ورقم البناء إلى 9.

## التحقق

تم تشغيل `tools/quality_gate.py` بالكامل بنجاح، مع إضافة اختبار:
`tools/invoice_ui_android_regression_smoke_test.py`.
