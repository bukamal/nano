# تقرير التعديل: نظام المظهر والوضع الليلي الذكي

## ما تم
1. **`src/nano_offline/core/theme.py`** — تحويل `Colors`/`Shadow` من كلاسات ثابتة القيم إلى
   كلاسات تُحل ألوانها ديناميكيًا وقت الاستخدام عبر metaclass، حسب الوضع الحالي (فاتح/داكن).
   بهذا لم يتطلب الأمر تعديل أي من مئات مواضع الاستخدام الحالية لـ `Colors.X` / `Shadow.X`
   عبر كل الشاشات — تستمر بنفس الاستدعاء، لكنها الآن تُرجع القيمة المناسبة للوضع الحالي.
   كذلك أُضيفت `LazyPalette` لإصلاح قوائم ألوان كانت ستتجمّد على وضع النهار فقط
   (`CATEGORY_PALETTE` في `items_view.py`، `CHIP_PALETTE` في `pos_view.py`)، وتحويل
   `SEVERITY_STYLE`/`STATUS_STYLES` لنفس المنطق.

2. **`src/nano_offline/core/theme_settings.py`** (جديد) — تخزين وقراءة تفضيل المظهر
   (فاتح/داكن/تلقائي حسب النظام) بالإضافة إلى ميزة "الجدولة الذكية": تفعيل الوضع الليلي
   تلقائيًا بين ساعتين محددتين (افتراضيًا 19:00–06:00) بغض النظر عن الاختيار الأساسي.
   القيم تُحفظ في جدول `settings` الموجود مسبقًا، بنفس نمط `sound_settings.py`.

3. **`src/main.py`** — قراءة وتطبيق المظهر عند بدء التشغيل وعند كل انتقال بين الشاشات
   (splash/تفعيل/دخول/الواجهة الرئيسية)، مع إعادة بناء كامل الواجهة فوريًا عند حفظ تغيير
   من لوحة الإعدادات (نفس آلية إعادة البناء الكاملة المستخدمة أصلًا بعد استعادة نسخة
   احتياطية)، واستماع اختياري (best-effort) لتغيّر سطوع نظام التشغيل لتحديث الوضع
   "التلقائي" تلقائيًا دون تدخل المستخدم.

4. **`src/nano_offline/views/admin_view.py`** — تبويب جديد "المظهر" ضمن لوحة الإدارة:
   - مفتاح ثلاثي (فاتح / داكن / تلقائي حسب النظام) بمكوّن `SegmentedToggle` الموجود مسبقًا.
   - مفتاح تفعيل الجدولة الزمنية الذكية مع اختيار ساعتي البداية والنهاية.
   - زر حفظ يُطبّق التغيير فورًا على كامل التطبيق دون الحاجة لإعادة تشغيله.

## التحقق الذي تم إجراؤه
- ✅ `python -m compileall src tools` على كامل المشروع — نجح بدون أخطاء.
- ✅ اختبار منطقي مستقل (بمحاكاة وحدة `flet`) لتأكيد: تبديل `Colors`/`Shadow` بين الوضعين،
  حل `SEVERITY_STYLE`/`STATUS_STYLES`/`LazyPalette` بشكل صحيح في كلا الوضعين، ورفع
  `AttributeError` على أي رمز غير معروف بدلًا من فشل صامت.
- ✅ اختبار منطقي لدالة `resolve_effective_mode` بما في ذلك حالة الجدولة الليلية الملتفة
  حول منتصف الليل (مثال: 19:00 → 06:00) وحالات التفضيل الصريح (فاتح/داكن) التي تتجاوز
  إعداد النظام.
- ⚠️ لم يتم تشغيل اختبارات الواجهة الفعلية (`phase*_flet_ui_contract_smoke_test.py`) لعدم
  توفر اتصال إنترنت لتثبيت حزمة `flet` في بيئة التنفيذ هنا. يُنصح بتشغيل
  `PYTHONPATH=src python tools/quality_gate.py` محليًا للتأكد البصري الكامل من التباين
  اللوني (contrast) في كل شاشة قبل الدمج، خصوصًا الشاشات التي لم يتم فتحها يدويًا هنا.

## ملاحظة مهمة
آلية التبديل تعتمد على أن كل شاشة (`*_view.py`) تُعيد بناء عناصرها بالكامل عند كل
`show_center()` (لا يوجد تخزين مؤقت للعناصر) — وهو ما كان صحيحًا بالفعل في كامل المشروع.
لو أُضيفت مستقبلًا شاشة تبني عناصرها مرة واحدة وتُعيد استخدامها، فلن تتحدّث ألوانها تلقائيًا
عند تبديل الوضع إلا بإعادة بناء كاملة، بنفس ما هو موضح في تعليقات `main.py`.
