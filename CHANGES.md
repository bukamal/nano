# إصلاح بناء 0.9.1 (CI)

## الملف: build_nano_apk.sh
- **التغيير:** إضافة `rm -rf build/flutter-packages` قبل حلقة البناء، وداخل حلقة إعادة المحاولة.
- **السبب:** `flet build apk` ينسخ الامتداد إلى `build/flutter-packages` ويعيد استخدامه بين المحاولات. أي ملف قديم عالق هناك — مثل `NanoGlanceWidget.kt` الذي حذفه إصلاح 0.9.1 من شجرة المصدر — ينجو من حلقة إعادة المحاولة ويُترجم ضد `build.gradle` الجديد الخالي من Glance، فيفشل `:flet_native_files:compileReleaseKotlin` بخطأ `Unresolved reference: glance/compose` في كل محاولة. تنظيف المجلد يضمن أن كل بناء يبدأ من نسخة نظيفة من شجرة المصدر.

## الملف: installer/build_nano_apk.sh
- **التغيير:** مزامنته مع `build_nano_apk.sh` في الجذر (كان يحمل نسخة 0.8.1 / رقم البناء 14 القديمة).
- **السبب:** النسخة داخل `installer/` كانت قديمة (0.8.1/14) بينما الجذر 0.9.1/22؛ أي استخدام لها يبني APK بإصدار قديم.

## ملاحظة
- شجرة المصدر نفسها سليمة: `verify_fix_091.py` يمر بنجاح (لا يوجد `NanoGlanceWidget.kt` ولا أي استيراد Glance). المشكلة كانت محتوى قديمًا في مجلد التدريج داخل CI، وليس في الكود المصدري.
