# Nano 0.8.3 — إصلاح ثانٍ لودجت الشاشة الرئيسية: الخطأ يعود بعد الحذف وإعادة الإضافة

## لماذا استمر الخطأ رغم إصلاح 0.8.2

إصلاح 0.8.2 (`android:exported="true"` + صورة المعاينة) كان ضروريًا لكنه لم
يكن كافيًا. تأكيد أن صورة المعاينة والوصف ظهرا بشكل صحيح في منتقي الودجت
يثبت أن البيان المُدمَج ونظام الموارد سليمان الآن — لكنه **لا** يثبت أن كود
Compose/Glance نفسه يعمل بلا استثناء، لأن صورة المعاينة والوصف مورَدان
ثابتان يقرأهما الـlauncher مباشرة من الـAPK، بمعزل تام عن نجاح أو فشل
`NanoGlanceWidget.provideGlance` وقت التشغيل الفعلي.

استمرار نفس رسالة "يتعذّر عرض المحتوى" **حتى بعد حذف الودجت وإعادة إضافتها
من جديد** ينفي فرضية "ودجت عالقة بحالة ربط قديمة فاسدة" (لأن الحذف
وإعادة الإضافة يُنشئان ربطًا جديدًا تمامًا)، ويشير مباشرة إلى استثناء
(exception) يتكرر في كل مرة يُعاد فيها بناء المحتوى — أي عطل داخل
`provideGlance`/`provideContent` نفسه.

## العطل الفعلي

```kotlin
Column(
    modifier = GlanceModifier
        .fillMaxSize()
        .padding(12.dp)
        .clickable(actionStartActivity(activity = mainActivityClass(context)))
) { ... }
```

`mainActivityClass(context)` تستخدم `Class.forName("${context.packageName}.MainActivity")`
عبر الـreflection لتفادي الاعتماد على وحدة التطبيق المُولَّدة وقت الترجمة
(انظر التعليق في الملف نفسه). هذا الاستدعاء **لم يكن ملفوفًا بأي
try/catch**، وكان يُنفَّذ في كل مرة تُعاد فيها بناء محتوى الودجت (كل تمرير
دوري، وكل استدعاء فوري من `push_home_widget`، وكل إعادة إضافة) — لا عند
الضغط عليها فقط. أي استثناء منه (`ClassNotFoundException` إن اختلف اسم
الفئة المُولَّدة فعليًا، أو غير ذلك) كان يُسقط بناء `provideContent` بالكامل
دون أي معالجة، وهذا بالضبط ما يجعل أندرويد يعرض شاشة الخطأ العامة
"يتعذّر عرض المحتوى" — بلا أي فرق ظاهري عن عطل حقيقي، ويتكرر مطابقًا في كل
مرة، بما يفسّر عودته رغم الحذف وإعادة الإضافة.

## التصحيحات في `NanoGlanceWidget.kt`

1. تفكيك بناء المحتوى إلى مصفوفتين من try/catch:
   - محاولة داخلية حول استدعاء `mainActivityClass` تحديدًا: عند فشلها،
     تُعرض الودجت بلا خاصية النقر لفتح التطبيق، بدل فقدان الودجت كاملة.
   - محاولة خارجية شاملة حول كامل بناء المحتوى: أي عطل غير متوقع آخر
     يُعرض بدلاً منه نص "تعذّر تحميل الأرقام، افتح التطبيق للتحديث" —
     حالة قابلة للتشخيص والتعافي، بدل شاشة أندرويد المبهمة.
2. تصحيح جانبي في `formatMoney()`: كان يُنتج نصًا مشوَّهًا لأي رصيد سالب
   (مثلاً `-500` تصبح `-,500`) بسبب عكس علامة السالب مع الفواصل — لم يكن
   هذا يسبب استثناءً، لكنه كان سيعرض رقمًا غير مقروء لو صار رصيد الصندوق
   سالبًا.
3. نفس الحماية أُضيفت في `NanoQuickSettingsTileService.onClick()` حول
   استدعاء `mainActivityClass` نفسه (يستخدمها زر القائمة السريعة أيضًا)
   بحيث يفشل الضغط بصمت بدل تعطّل واجهة النظام.

## حدود هذا الإصلاح المهمة

لم تتوفر في بيئة التطوير الحالية أدوات Flutter/Gradle/جهاز Android فعلي
لبناء APK والتقاط `adb logcat` عند حدوث العطل الفعلي — هذا امتداد لنفس
القيد المذكور في `README.md` و`PHASE10_HOME_WIDGET_GLANCE_AR.md`. التشخيص
أعلاه مبني على قراءة الكود ومطابقة السلوك الموصوف (تكرار العطل رغم
الحذف/إعادة الإضافة) مع أثر معروف لاستثناء غير مُعالَج داخل
`provideContent`، وليس على سجلّ عطل فعلي من الجهاز.

**إن استمرت رسالة "يتعذّر عرض المحتوى" حتى بعد هذا الإصلاح**، الخطوة
التالية الأكثر فائدة هي التقاط `adb logcat` وقت إضافة الودجت مباشرة
(مثلاً `adb logcat | grep -iE "glance|homewidget|AppWidget"`) لأن نص
الاستثناء الفعلي سيحدد السبب بدقة بدل الاستمرار في التخمين.

- Version: 0.8.3
- Android build number: 15
