# تقرير: حذف المواد — نطاق ضيق، مطابق لما اتُّفق عليه

## طبقة البيانات (`item_repository.py`)
- `delete(item_id)` جديدة كليًا (لم تكن موجودة إطلاقًا سابقًا — كان هناك
  `create`/`update` فقط).
- الحماية: فحص صريح على `invoice_lines` و`inventory_movements` قبل الحذف،
  بنفس اتفاقية `PartyRepository.delete`/`delete_category`/`delete_unit`.
- **نطاق الحذف الفعلي أضيق مما يبدو للوهلة الأولى:** أي مادة أُضيفت برصيد
  افتتاحي (`quantity > 0`) تحصل تلقائيًا على حركة `adjustment` عند الإنشاء
  (`create()` في نفس الملف) — أي تصبح "مستخدمة" فورًا حتى قبل أي فاتورة.
  المواد القابلة للحذف الآمن هي حصرًا:
  - مادة خدمة (لا تُنشئ حركة مخزون أبدًا)، أو
  - مادة مخزون بدأت برصيد افتتاحي = صفر،
  
  ولم تُستخدم بعد في أي فاتورة شراء أو بيع.
- `list()` الآن تُرجع عمود `has_activity` محسوبًا (`EXISTS` على الجدولين
  معًا) بدل استعلام إضافي منفصل لكل مادة — نفس أسلوب `item_count` المُضاف
  سابقًا للتصنيفات/الوحدات.
- **ملاحظة أداء صادقة:** لا يوجد فهرس (`INDEX`) على `invoice_lines.item_id`
  حاليًا في المخطط (يوجد فقط على `invoice_id`). على حجم بيانات هذا التطبيق
  (تجاري صغير، عدد مواد محدود) هذا غير محسوس، لكنه يستحق فهرسًا لو كبرت
  قاعدة البيانات كثيرًا مستقبلًا — لم أُضِف هجرة مخطط (migration) لهذا الآن
  لأنه خارج نطاق طلب الواجهة، ويحتاج قرارًا منفصلاً حول ترقيم الإصدار.
- اختبار دائم جديد: `tools/item_delete_guard_smoke_test.py` (أُضيف إلى
  `quality_gate.py`) يغطي: حذف خدمة غير مستخدمة، حذف مادة برصيد صفري غير
  مستخدمة، رفض حذف مادة برصيد افتتاحي (قبل أي فاتورة)، ورفض حذف مادة
  استُخدمت في فاتورة فعلية.

## الواجهة (`items_view.py`)
- Swipe-to-delete على صف المادة في القائمة الرئيسية — **فقط للمواد التي
  `has_activity=False`**. مادة استُخدمت سابقًا لا تُغلَّف بـ `Dismissible`
  إطلاقًا، فلا تظهر لها إيماءة حذف من الأساس (بنفس فلسفة العملاء والتصنيفات:
  لا نعرض إجراءً مضمون الفشل).
- السحب لا يحذف مباشرة — يفتح حوار تأكيد؛ الإلغاء أو فشل الحذف لأي سبب
  يعيدان رسم القائمة (`refresh()`) فيعود الصف المسحوب بصريًا تلقائيًا، لأن
  لا شيء حُذف فعليًا من القائمة المصدر.
- لم أُضِف زر حذف داخل شيت تفاصيل المادة (`show_item_detail`) عمدًا — تركت
  الحذف في مكان واحد فقط (السحب من القائمة) بدل تكرار نفس المنطق في مكانين،
  لأن معيار الأهلية (`has_activity`) متاح مسبقًا في بيانات القائمة دون
  استعلام إضافي، بينما شيت التفاصيل يحسب `stats` بطريقة مختلفة قد لا تطابق
  المعيار حرفيًا.

## التحقق الآلي
```
PYTHONPATH=src python tools/quality_gate.py
...
quality_gate passed   (٤٢ اختبارًا، شامل الاختبار الجديد)
```

## قيد صادق (متكرر)
نفس القيد المعتاد: لم يُختبر على Flet حي. يستحق تجربة سريعة على الجهاز،
خصوصًا التأكد أن السحب على صف مادة لا يتعارض بصريًا مع النقر لفتح التفاصيل
(الصف نفسه له كل من `on_click` و`Dismissible` معًا — نفس النمط المستخدم في
العملاء، لكن يستحق تأكيدًا بصريًا واحدًا لا أكثر).
