# تقرير: ثلاث نغمات جديدة — تسجيل دخول، تنبيه مركز الإشعارات، باركود غير موجود

نظام الصوت كان يحتوي سبع نغمات (success/error/warning/info/scan/save/delete).
أُضيفت ثلاث نغمات جديدة لأحداث لم تكن مغطاة:

## 1. `login` — تسجيل الدخول
نغمة ترحيب دافئة (ثلاث نغمات صاعدة C5-E5-G5)، أطول وأدفأ من نغمة "نجاح".
مربوطة في `login_view.py` عند كل مسار نجاح فعلي: الدخول العادي، الدخول
السريع بـ PIN، والدخول السريع بالنمط — ثلاث نقاط استدعاء لـ `on_success()`.

## 2. `notify` — تنبيه جديد بمركز الإشعارات
نغمة هادئة غير ملحّة (F#5 ثم C#5، خُمس هابط). مربوطة في
`notifications_view.py`'s `refresh_badge()` — **لكن فقط عند ارتفاع فعلي**
في عدد التنبيهات غير المقروءة عن آخر قيمة معروفة، وليس عند كل استدعاء
لـ`refresh_badge()` نفسه (يُستدعى أيضًا عند كل تنقل بين التبويبات في
`main.py`، فتشغيله في كل مرة كان سيُصدر صوتًا مزعجًا مع كل ضغطة). أول
استدعاء في الجلسة يُثبّت خط الأساس بصمت دون تشغيل صوت.

## 3. `barcode_error` — باركود غير موجود
نغمة قصيرة حادة (طقّتان منخفضتان G3)، منفصلة عن نغمة "خطأ" العامة، لتُقرأ
فورًا كـ"لا توجد مطابقة" أثناء المسح السريع. استبدلت `sound_kind="error"`
في ثلاث شاشات:
- `pos_view.py` — `_offer_create_item` (سواء ظهر مربع "إنشاء مادة جديدة" أم لا)
- `stocktake_view.py` — مسار الباركود غير الموجود في الجرد المستمر
- `invoice_view.py` — مسار الباركود غير الموجود داخل الفاتورة

## الربط الكامل (نفس النمط المتّبع مع نغمة `delete` سابقًا)
- `src/assets/sounds/{login,notify,barcode_error}.wav` +
  نفس الملفات في `extensions/flet_native_files/.../assets/sounds/`.
- `core/sound.py`: أُضيفت الثلاثة لـ `SoundKind` و`_SOUND_KINDS`.
- `core/sound_settings.py`: ثلاثة مفاتيح جديدة (افتراضيًا: مفعّلة كلها).
- `sound_pool.dart`: ثلاثة إدخالات جديدة في `kSoundAssetPaths`.
- شاشة الإدارة ← تبويب "الصوت": ثلاثة مفاتيح تفعيل + أزرار معاينة + أسطر
  في لوحة التشخيص.
- `tools/sound_system_smoke_test.py` محدَّث بالكامل للنغمات الثلاث
  الجديدة في كل المواضع الأربعة (الإعدادات، ملف WAV بايثون، خريطة Dart،
  ملف WAV Flutter) — **الاختبار ناجح**.

## لم يُختبر على جهاز حقيقي
كالمعتاد، لا توجد بيئة Flutter/Android هنا. التحقق اقتصر على `py_compile`
لكل الملفات المعدَّلة وتشغيل `sound_system_smoke_test.py` (ناجح) إضافة
إلى `core_smoke_test.py` و`stocktake_continuous_scan_smoke_test.py`
(ناجحان أيضًا، للتأكد من عدم كسر أي شيء آخر).
