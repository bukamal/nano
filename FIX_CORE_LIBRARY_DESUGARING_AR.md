# تقرير: إصلاح فشل بناء APK — "core library desugaring" بعد إضافة ميزة الإشعارات

## السبب الجذري
سجلّ CI (المرفق `logs_90082680400.zip`) يُظهر فشل Gradle في مهمة
`:app:checkReleaseAarMetadata` بنفس الرسالة في المحاولات الثلاث:

```
Dependency ':flutter_local_notifications' requires core library
desugaring to be enabled for :app.
```

هذا بدأ بالضبط عند إضافة ميزة "الإشعار الذكي عند إغلاق التطبيق" —
`extensions/flet_native_files` أضاف تبعية `flutter_local_notifications`
(انظر `pubspec.yaml`)، وإصدارات هذه الحزمة الحديثة تستخدم Java 8+ APIs على
أندرويد، ما يستوجب تفعيل `isCoreLibraryDesugaringEnabled` وإضافة تبعية
`coreLibraryDesugaring` في `android/app/build.gradle.kts` — وهو ملف
يُنشئه Flet تلقائيًا ولا يوجد له مفتاح مباشر في `pyproject.toml`.

## لماذا الإصلاح القديم في `build_nano_apk.sh` لم يعمل
كانت هناك آلية معقّدة لتعديل `build/flutter/android/app/build.gradle.kts`
مباشرة بعد إنشائه: أولًا بعد فشل المحاولة، ثم (لما تبيّن أن ذلك "متأخر
جدًا") عبر "مراقب" يعمل في الخلفية أثناء تنفيذ `flet build apk` نفسه،
يرصد تغيّر الملف ويُصلحه فورًا.

**كلا الأسلوبين لم يعملا فعليًا.** السجلّ يُثبت ذلك بوضوح: نفس خطأ
Gradle تكرر حرفيًا في المحاولات الثلاث، ولم يظهر ولو مرة واحدة أي من
أسطر المراقب الخاصة به (`[watcher] ...`، `Checking ...`، `Patched ...`)
— حتى أول سطر منها وهو `echo` بسيط لا يعتمد على أي عملية خارجية ومن
المفترض أن يظهر فورًا. هذا يعني أن السباق (race) لم يكن قريبًا من
النجاح في أي محاولة؛ المشكلة أعمق من ضبط التوقيت، والاعتماد على "اللحاق"
بملف تُنشئه/تُعيد إنشاءه أداة خارجية (Flet) هش بطبيعته.

## الإصلاح الجديد
استبدلت الآلية بالكامل بـ **Gradle init script**: أي ملف
`*.gradle.kts` داخل `~/.gradle/init.d/` يطبّقه Gradle تلقائيًا على *كل*
بناء يُنفَّذ على هذه الآلة، بصرف النظر عن عدد مرات إعادة إنشاء Flet
للمشروع أو عدد محاولات إعادة المحاولة في حلقة `build_nano_apk.sh`. هذا
يُلغي مشكلة السباق من أساسها لأننا لم نعد نعدّل ملفًا مُولَّدًا ونأمل
بقاءه، بل نُخبر Gradle نفسه — مرة واحدة قبل البناء — أن يُفعّل
desugaring لأي وحدة Android Application يبنيها.

تم تطبيق التعديل في:
- `build_nano_apk.sh` (الملف الذي يُنفَّذه CI فعليًا)
- `installer/build_nano_apk.sh` (نسخة مطابقة كانت موجودة مسبقًا)

كلاهما كانا متطابقين حرفيًا قبل الإصلاح، فطُبِّق نفس التعديل على الاثنين
حفاظًا على التطابق.

## ملاحظة: ملف لم يكن موجودًا في الأرشيف المرفوع
عند تشغيل `tools/quality_gate.py` على المشروع المرفوع، خمسة اختبارات
تفشل بسبب `FileNotFoundError` على `.github/workflows/build-android-apk.yml`
— هذا الملف غير موجود إطلاقًا في `nano_smart_barcode_fixed.zip` (على
الأرجح استُبعد مجلد `.github` المخفي عند الضغط). هذا لا علاقة له بميزة
الإشعارات ولم ألمسه، لأنني لا أملك محتواه الأصلي لإعادة بنائه بثقة.
كذلك ظهرت فشلين آخرين غير مرتبطين (`SCHEMA_VERSION == 9` و
`DocumentService._shell()` بمعامل ناقص) — هذه أيضًا خارج نطاق طلبك
(خطأ البناء بعد إضافة الإشعارات) ولم أُعدّلها.

## التحقق
- `bash -n build_nano_apk.sh` و`bash -n installer/build_nano_apk.sh` ⇐ لا أخطاء صياغة.
- محاكاة معزولة لسلوك `flet build apk` أثبتت أن آلية المراقب القديمة
  تعمل نظريًا في أبسط الحالات، ما يرجّح أن الفشل الفعلي في CI مرتبط
  ببيئة التشغيل الحقيقية لا بخطأ منطقي بسيط في الكود — وهو سبب إضافي
  لتفضيل حل لا يعتمد على السباق أصلًا بدل محاولة تصحيح توقيته.
