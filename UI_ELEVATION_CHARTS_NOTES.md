# تقرير: نظام ارتفاع (Elevation) موحّد + مخططات دائرية (Donut) في اللوحة والتقارير

طبّقت أول اقتراحين آمنين من قائمة "أفكار متقدمة" السابقة — الاثنان لا يعتمدان
على توقيت حركة حي (motion) لذا لا يحتاجان معاينة بصرية دقيقة لضبطهما.

## 1) نظام ظلال موحّد (`Shadow` في `theme.py`)

قبل هذا التعديل كان هناك ظل واحد فقط في كامل المشروع (مكتوب يدويًا داخل
`dashboard_view._metric`)، وبقية البطاقات (حدود `Colors.BORDER` فقط) مسطحة
بصريًا. أضفت كلاس `Shadow` بجانب `Spacing`/`Radius` الموجودين أصلًا بنفس
الروح: 3 درجات فقط (`SM` / `MD` / `LG`).

**قرار تصميمي مهم:** لم أستخدم `ft.Colors.with_opacity()` رغم أنه الأسلوب
"الصحيح" نظريًا للظلال شبه الشفافة — لأنني لا أستطيع تشغيل Flet هنا للتأكد من
سلوكه في هذا الإصدار (`flet==0.28.3`). بدلاً من ذلك استخدمت *نفس* نمط الظل
الحرفي (`color=Colors.BORDER`) الذي كان يعمل بالفعل في `dashboard_view.py`
قبل التعديل — فهو الوحيد المؤكد أنه يُصنَّف Flutter بشكل صحيح على بناء فعلي.
أمان أعلى على حساب دقة لونية بسيطة جدًا (لا فرق يُذكر بصريًا).

**طُبّق على:**
- `dashboard_view.py`: بطاقات المؤشرات (KPI)، الإجراءات السريعة، المبيعات/المشتريات، التنبيهات، آخر الفواتير.
- `reports_view.py`: `_metric()` و `_card()` (تنطبق تلقائيًا على كل التقارير الخمسة).

**تحديث: تم التعميم على كامل المشروع.** راجعت يدويًا (سطرًا سطرًا، ليس
استبدالًا آليًا أعمى) كل الـ29 موضع المتبقي عبر `main.py`, `invoice_view.py`,
`items_view.py`, `parties_view.py`, `finance_view.py`, `security_view.py`,
`activation_view.py`, `admin_view.py`، وصنّفت كل حالة حسب دورها البصري:

| النوع | الدرجة | أمثلة |
|---|---|---|
| بطاقات مؤشرات/إحصاءات وصفوف قوائم | `Shadow.SM` | بطاقات المواد، العملاء، سندات القبض/الصرف، المصروفات، أسطر الفاتورة |
| أسطح "طافية" فوق المحتوى (Drawer، Bottom Sheet، بطاقة تفعيل قائمة بذاتها) | `Shadow.LG` | القائمة الجانبية، شيت "المزيد"، بطاقة التفعيل/الدخول |
| استُثنيت عمدًا | — | الهيدر العلوي اللاصق (حده السفلي تصميم مقصود لا بطاقة)، الشريط السفلي للتنقل (له ظل مخصص أصلي في الكود قبل أي تعديل مني — لم ألمسه) |

**التحقق النهائي:** كتبت سكربت فحص صغير يمسح كل مواضع `bgcolor=Colors.WHITE`
في `src/` ويتأكد من وجود `shadow=Shadow` ضمن نفس الكتلة — النتيجة: كل المواضع
مغطاة عدا الاستثناءين المذكورين أعلاه عمدًا.

## 2) مخططات دائرية (Donut) — أول استخدام لـ `ft.PieChart` في المشروع

- **اللوحة الرئيسية**: دونات صغيرة (52px) بجانب عنوان بطاقة "المبيعات
  والمشتريات" تُظهر نسبة المبيعات إلى المشتريات بصريًا، مكمّلة لأشرطة `flow_row`
  الموجودة (الأشرطة للقيم المطلقة، الدونات للنسبة).
- **تقرير قائمة الدخل (P&L)**: دونات + ليجند (أعلى 6 تصنيفات) فوق قائمة
  "المصروفات حسب التصنيف" النصية الموجودة أصلًا — أضفتها ولم أستبدل القائمة.

كلا الاستخدامين يتعاملان مع حالة "لا بيانات" بأمان (دونات رمادية فارغة في
اللوحة، واختفاء الدونات كليًا في التقرير إن لم توجد مصروفات موجبة).

## التحقق الذي تم إجراؤه
- ✅ `ast.parse()` + `python -m compileall src tools` — نجح بدون أخطاء.
- ✅ `tools/core_smoke_test.py` — نفس النتائج المعتادة (`purchase=1 sale=2 customer_balance=50.0 supplier_balance=40.0 stock=12.0`).
- ⚠️ **القيد المعتاد**: لا إنترنت هنا لتثبيت `flet` وتشغيل معاينة حية، و`ft.PieChart`
  تحديدًا لم يُستخدم في هذا المشروع من قبل — فحصت التوقيع مقارنة بمعرفتي بواجهة
  Flet القياسية (`sections`, `sections_space`, `center_space_radius`) وتجنّبت
  عمدًا أي معامل غير متأكد من دعمه (مثل `animate` على `PieChart` تحديدًا).
  **يُنصح بتشغيل التطبيق ومعاينة الشاشتين (اللوحة الرئيسية + تقرير قائمة
  الدخل) قبل الدمج النهائي.**

## الملفات المعدّلة
```
src/nano_offline/core/theme.py             (تحديث - كلاس Shadow جديد)
src/nano_offline/views/dashboard_view.py   (تحديث - ظلال + دونات المبيعات/المشتريات)
src/nano_offline/views/reports_view.py     (تحديث - ظلال + دونات المصروفات)
src/main.py                                 (تحديث - ظلال: زر الإشعارات، الدرج الجانبي، شيت المزيد، بطاقة التفعيل)
src/nano_offline/views/invoice_view.py     (تحديث - ظلال: مؤشرات، صفوف الفواتير، أسطر الفاتورة)
src/nano_offline/views/items_view.py       (تحديث - ظلال: إحصاءات، صفوف المواد)
src/nano_offline/views/parties_view.py     (تحديث - ظلال: إحصاءات، صفوف العملاء/الموردين)
src/nano_offline/views/finance_view.py     (تحديث - ظلال: مؤشرات، صفوف السندات/المصروفات/الكشوف)
src/nano_offline/views/security_view.py    (تحديث - ظلال: بطاقة القسم)
src/nano_offline/views/activation_view.py  (تحديث - ظلال: بطاقة التفعيل)
src/nano_offline/views/admin_view.py       (تحديث - ظلال: بطاقة القسم)
```

## الخطوة التالية المقترحة
- Dark Mode و Motion لا يزالان يحتاجان جلسة مخصصة بمعاينة حية (نفس القيد السابق).
- بعدها: Bottom Sheets بدل الـ Dialogs، أو Swipe Actions على صفوف القوائم.
