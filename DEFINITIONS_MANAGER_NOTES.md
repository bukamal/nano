# تقرير: تحديث "التصنيفات والوحدات" — من نموذج إضافة فقط إلى إدارة كاملة

## ما تغيّر في طبقة البيانات (`definitions_repository.py`)
كانت الطبقة تدعم **الإضافة فقط**. أضفت:
- `rename_category` / `rename_unit`
- `delete_category` / `delete_unit` — بنفس أسلوب `PartyRepository.delete`
  تمامًا: تحقّق صريح قبل الحذف (`SELECT ... LIMIT 1`) بدل الاعتماد فقط على
  `ON DELETE RESTRICT` في المخطط، حتى تظهر رسالة عربية واضحة بدل خطأ
  `IntegrityError` خام.
- `list_categories` / `list_units` الآن تُرجع `item_count` لكل عنصر (عدد
  المواد المرتبطة)، عبر subquery واحد بدل استعلام منفصل لكل عنصر.
- **تفصيل مهم لم يكن واضحًا من قراءة الواجهة القديمة:** الوحدة يمكن أن
  تكون مرتبطة بمادة بطريقتين — كوحدة أساسية (`items.base_unit_id`) أو كوحدة
  بديلة/تحويل (`item_units.unit_id`). كلا الحالتين يُفحصان في `delete_unit`
  وفي حساب `item_count`؛ تجاهل إحداهما كان سيسمح بحذف وحدة لا تزال مستخدمة
  فعليًا في تحويلات مادة ما.
- كل عملية (إضافة/تعديل/حذف) تُسجَّل في `audit_log`، بنفس اتفاقية بقية
  المستودعات (`item_repository`, `party_repository`).
- اختبار جديد دائم: `tools/definitions_management_smoke_test.py` (مُضاف
  لـ `quality_gate.py`) يغطي: العدّ، التعديل، الحذف الناجح، رفض الحذف
  للتصنيف/الوحدة الأساسية المستخدمة، رفض حذف وحدة مستخدمة كتحويل بديل فقط،
  ورفض اسم فارغ عند التعديل.

## ما تغيّر في الواجهة (`items_view.py`)
استُبدل حوار `AlertDialog` الثابت (إضافة فقط، بلا عرض للموجود) بـ
`BottomSheet` حي، بنفس الأسلوب المستخدم فعليًا في تفاصيل المادة و"المزيد" —
وليس نمطًا جديدًا غير مجرَّب:

- **تبويب علوي** (شريحتان) يبدّل بين "التصنيفات" و"الوحدات"، بنفس أسلوب
  chip المستخدم أصلًا لفلاتر الفواتير.
- **قائمة حية** لكل عنصر موجود، مع:
  - شارة استخدام ("٤ مواد" أو "غير مستخدم") محسوبة من `item_count`.
  - زر تعديل (يفتح حوار تعديل صغير — أبقيت هذا كـ AlertDialog منفصل عمدًا،
    وليس تعديلًا داخل الصف نفسه؛ تعديل نصي inline داخل قائمة قابلة للسحب
    والتمرير له حالات حافة يصعب التحقق منها بصريًا في بيئتي الحالية، بينما
    حوار منفصل مضمون السلوك وسريع الاستخدام بنفس القدر).
  - **Swipe-to-delete، لكن فقط للعناصر غير المستخدمة.** عنصر مستخدم لا
    يُغلَّف بـ `Dismissible` إطلاقًا — لا تظهر له إيماءة حذف من الأساس،
    بدل عرض إيماءة ستفشل دائمًا. هذا امتداد لنفس فلسفة الأمان من ميزة
    Swipe للعملاء: القدرة على الحذف تُشتق من الحالة الفعلية، لا من التخمين.
  - السحب لا يحذف مباشرة — يفتح نفس حوار تأكيد مركزي، وإلغاء التأكيد (أو
    فشل الحذف لأي سبب) يُعيد رسم القائمة فيُعيد الصف المسحوب بصريًا.
- **حقل إضافة مضغوط** بأسلوب pill (حواف دائرية 20px، بلا تسمية منفصلة،
  فقط hint text) بدل حقل + زر منفصلين تحته — يوفّر مساحة رأسية.
- إغلاق الشيت يزامن متغيّرات `categories`/`units` الملتقطة أعلى الدالة (التي
  تُستخدم في قوائم `SearchSelect` داخل محرر المادة) — دون الحاجة لإعادة بناء
  الشاشة كاملة عبر `self.show_center()` كما كان يحدث سابقًا بعد كل إضافة.

## قيد صادق (نفس القيد المتكرر)
هذا لم يُختبر على Flet حي — لا الحركة الضمنية، ولا سلوك `Dismissible` مع شيت
مضمّن داخل شيت آخر إن فُتح محرر المادة فوقه (لم يحدث تصميميًا: زر التصنيفات
والوحدات موجود في شاشة القائمة فقط، ليس داخل محرر المادة نفسه). يُنصح
بتجربة سريعة على الجهاز، خصوصًا التمرير المحدود الارتفاع (`height=280`)
لقائمة العناصر داخل الشيت.

## التحقق الآلي
```
PYTHONPATH=src python tools/quality_gate.py
...
quality_gate passed   (٤١ اختبارًا، شامل الاختبار الجديد)
```
