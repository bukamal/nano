# المرحلة 2 — محرر الفواتير الأوفلاين

## الهدف

تحويل Vertical Slice للفاتورة في المرحلة الأولى إلى مركز فواتير فعلي يدعم البيع والشراء، البنود المتعددة، الوحدات، الدفعة، التعديل والحذف مع سلامة المخزون والذمم عند تعديل فواتير تاريخية.

## ما تم تنفيذه

### 1. مركز الفواتير

- قائمة فواتير البيع والشراء.
- اسم الطرف، الإجمالي، المدفوع، المتبقي وحالة السداد.
- إنشاء فاتورة بيع أو شراء.
- تعديل فاتورة قائمة.
- حذف نهائي بعد تأكيد.

### 2. محرر متعدد البنود

كل بند يدعم:

- مادة/خدمة.
- بيان.
- وحدة.
- كمية.
- سعر.
- إجمالي تلقائي.
- حذف البند.

السعر الافتراضي للوحدة الإضافية يساوي سعر الوحدة الأساسية × معامل التحويل، مع إمكانية تعديل السعر يدويًا.

### 3. عقد الوحدات

معامل التحويل لا يؤخذ من الواجهة كمصدر موثوق عند ربط البند بمادة. `InvoiceService` يعيد حله من قاعدة البيانات:

- الوحدة الأساسية = 1.
- الوحدة الإضافية = `item_units.conversion_factor`.
- أي وحدة غير معرفة للمادة تُرفض.

هذا يمنع حفظ كمية أساس خاطئة بسبب قيمة UI تالفة أو معدلة.

### 4. الفواتير النقدية والآجلة

- يسمح بفاتورة بلا عميل/مورد عندما تكون مدفوعة بالكامل.
- إذا بقي مبلغ مستحق، يجب تحديد العميل لفاتورة البيع أو المورد لفاتورة الشراء.
- لا يُنشأ رصيد مجهول الطرف.

### 5. تعديل وحذف الفاتورة التاريخية

بدل عكس عدة جداول يدويًا بعمليات منفصلة، أصبحت الفاتورة مصدر الحقيقة. بعد Create/Update/Delete يقوم النظام داخل نفس SQLite transaction بإعادة بناء:

1. المخزون الحالي.
2. التكلفة المتوسطة.
3. تكلفة كل سطر بيع `unit_cost/cost_amount`.
4. حركات المخزون المشتقة من الفواتير.
5. دفعات الفواتير المشتقة من `paid_amount`.
6. أرصدة العملاء والموردين.
7. قيود دفتر الأستاذ الخاصة بالفواتير والدفعات.

إذا أدى تعديل تاريخي إلى مخزون سالب في حركة لاحقة، تفشل العملية كلها ويتم Rollback، وتبقى الفاتورة القديمة وحساباتها كما كانت.

### 6. الرصيد الافتتاحي للمخزون

أضيف إلى جدول `items`:

- `opening_quantity`
- `opening_unit_cost`

Schema Version أصبح `2`.

الترحيل من Phase 1 يقرأ حركات `adjustment` القديمة ويستنتج منها الرصيد والتكلفة الافتتاحية دون حذف البيانات الحالية. الرصيد الافتتاحي يصبح Baseline يسبق جميع الفواتير مهما كان تاريخ إدخالها.

### 7. Responsive Flet UI

- الهاتف: `NavigationBar`.
- الشاشات بعرض 900px أو أكثر: `NavigationRail`.
- حقول الفاتورة وبنودها تستخدم `ResponsiveRow`.
- القوائم تُعرض كبطاقات مناسبة للشاشة الصغيرة بدل جدول عريض.

### 8. اختبار Phase 2

تم اختبار سيناريو يتضمن:

- مخزون افتتاحي 10 وحدات بتكلفة 10.
- وحدة أساسية «قطعة».
- وحدة «كرتون» بمعامل 5.
- شراء تاريخي بوحدة الكرتون.
- بيع متعدد البنود: مادة + خدمة.
- دفعة جزئية.
- تعديل فاتورة شراء تاريخية وإعادة احتساب COGS.
- محاولة تعديل بيع إلى كمية غير متاحة والتأكد من Rollback الكامل.
- حذف الشراء وإعادة البناء.
- حذف البيع وعودة المخزون والذمم والـCash إلى الحالة الصحيحة.
- `PRAGMA integrity_check = ok`.

## ما لم ينفذ في هذه المرحلة

- سند قبض مستقل.
- سند صرف مستقل.
- تسديد فاتورة بعد إنشائها من شاشة منفصلة.
- المصروفات UI.
- كشف حساب عميل/مورد.
- تقارير المخزون والأرباح التفصيلية.
- طباعة الفاتورة/PDF.
- Backup/Restore.
- المستخدمون والصلاحيات المحلية.
- Activation API والتوقيع الرقمي.
