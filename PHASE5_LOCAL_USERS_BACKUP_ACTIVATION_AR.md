# Phase 5 — المستخدمون المحليون والنسخ الاحتياطي والتفعيل

## الهدف
إضافة طبقة إدارة وأمان محلية فوق التطبيق الأوفلاين بدون إدخال أي قاعدة بيانات سحابية في المسار المحاسبي. الاتصال بالشبكة محصور في طلب التفعيل فقط، وبعد نجاحه يتم التحقق من الترخيص محليًا.

## المنفذ

### 1. مستخدمون محليون وصلاحيات
أضيف جدول `users` و`AuthService` مع:

- إنشاء مدير أول عند أول تشغيل بدل كلمة مرور افتراضية معروفة.
- تسجيل دخول محلي بالكامل.
- كلمات المرور محفوظة بصيغة PBKDF2-HMAC-SHA256 مع Salt عشوائي، وليست نصًا صريحًا.
- قفل مؤقت بعد خمس محاولات دخول فاشلة.
- أربعة أدوار:
  - `admin`: كامل الصلاحيات + الإدارة.
  - `accountant`: المحاسبة والفواتير والتقارير دون إدارة المستخدمين.
  - `sales`: الرئيسية والعملاء والمواد والفواتير.
  - `viewer`: الرئيسية والتقارير فقط.
- إمكانية إنشاء مستخدمين وتعطيلهم من شاشة الإدارة.
- منع تعطيل المستخدم الحالي ومنع تعطيل آخر مدير نشط.

### 2. ربط سجل التدقيق بالمستخدم
تم توسيع `audit_log` بحقلَي:

- `user_id`
- `username`

وتمت إضافة Trigger يستخدم هوية الجلسة الحالية تلقائيًا عند إدراج أي سجل تدقيق من الخدمات القديمة، لذلك لا يلزم تعديل كل عملية مالية على حدة حتى نعرف من أنشأ/عدّل/حذف العملية.

### 3. النسخ الاحتياطي
أضيف `BackupService` وتنسيق محلي بامتداد:

`*.qeidbackup`

النسخة تحتوي:

- قاعدة SQLite كاملة.
- المستخدمين المحليين.
- الحركات المحاسبية والمخزون والتقارير المصدرية.
- `manifest.json` يتضمن إصدار Schema وبصمة SHA-256.

لا يتم تضمين `license_state` في النسخة لأن الترخيص مرتبط بالجهاز وليس بالبيانات.

قبل إنشاء النسخة يتم تنفيذ WAL checkpoint ثم SQLite Backup API، وبعدها:

1. فحص `PRAGMA integrity_check`.
2. حساب SHA-256.
3. تغليف القاعدة والـManifest داخل ملف واحد.

### 4. الاسترجاع الآمن
قبل الاسترجاع:

1. يتم التحقق من بنية ملف النسخة.
2. مطابقة SHA-256.
3. فحص سلامة SQLite.
4. رفض نسخة منشأة بـSchema أحدث من التطبيق.
5. إنشاء Backup أمان تلقائي للحالة الحالية.

بعد الاسترجاع:

- تشغل Migrations غير الهدامة تلقائيًا.
- يبقى ترخيص الجهاز الحالي محفوظًا ولا يتم استبداله بترخيص من النسخة.
- يعاد `integrity_check` بعد العملية.

### 5. التفعيل الأونلاين فقط
أعيد بناء `LicenseService` ليعتمد نموذج توقيع غير متماثل:

- التطبيق يحمل المفتاح العام RSA فقط.
- المفتاح الخاص يبقى على خادم التفعيل مستقبلًا.
- التوقيع `RSASSA-PKCS1-v1_5 / SHA-256` ويتم التحقق منه محليًا باستخدام Python Standard Library فقط.
- رمز التفعيل مرتبط بـ`device_id` محلي ثابت خارج قاعدة البيانات، لذلك نقل Backup إلى جهاز آخر لا ينقل الترخيص.
- بعد التفعيل لا توجد حاجة للشبكة للتحقق المعتاد.

طلب التفعيل الشبكي يرسل فقط:

- `license_key`
- `device_id`
- `app_version`

ولا يرسل العملاء أو الفواتير أو المخزون أو التقارير.

### 6. واجهة الإدارة
أضيفت شاشة `الإدارة` للمدير وتحتوي على:

- إدارة المستخدمين.
- إنشاء نسخ داخل مجلد Backup المحلي.
- استرجاع نسخة بعد تأكيد.
- إعداد عنوان خادم التفعيل HTTPS والمفتاح العام.
- تشغيل التفعيل.
- عرض حالة الترخيص.
- عرض آخر 100 حركة من سجل التدقيق.
- تسجيل الخروج.

كما أصبح التطبيق يعرض شاشة إنشاء المدير الأول أو تسجيل الدخول قبل فتح الـShell الرئيسي.

## قاعدة البيانات
تم رفع:

`SCHEMA_VERSION = 4`

مع Migration غير هدام من Schema 3 يحافظ على سجل التدقيق القديم ويضيف حقول المستخدم الجديدة.

## الإصدار

- التطبيق: `0.5.0`
- `build_number = 5`
- `Flet = 0.28.3`

## الاختبارات الجديدة

- `phase5_schema_migration_smoke_test.py`
- `phase5_auth_security_smoke_test.py`
- `phase5_backup_restore_smoke_test.py`
- `phase5_license_offline_smoke_test.py`
- `phase5_admin_ui_contract_smoke_test.py`
- `phase5_project_contract_smoke_test.py`

كما تم تعديل اختبارات العقود القديمة حتى تتحقق من حد أدنى للإصدار/Schema بدل تثبيت مرحلة قديمة حرفيًا.

## نتيجة Quality Gate
جميع اختبارات Phase 1–5 نجحت، بما فيها الترحيل والحسابات والمخزون والدفعات والتقارير والمستخدمون والنسخ والتوقيع المحلي.

## حدود هذه المرحلة

- ملف Backup الحالي **موثق ومتحقق من سلامته لكنه غير مشفر**؛ يجب حفظه في مكان آمن. التشفير سيضاف كميزة مستقلة بعد اختيار مكتبة متوافقة مع Android بدل إدخال Crypto Native قد يعطل بناء Flet.
- شاشة Phase 5 تنشئ النسخ داخل مجلد التطبيق. مشاركة النسخة إلى Downloads/Drive أو اختيار ملف خارجي ستعالج في مرحلة Android، خصوصًا لأن Flet 0.28.3 لديه قيود مع مسارات Android Document Provider.
- خادم التفعيل الحقيقي والمفتاح الإنتاجي لم ينشآ بعد؛ الطبقة المحلية والعقد الشبكي جاهزان.
- لا يتم فرض إيقاف التطبيق عند غياب الترخيص أثناء التطوير الحالي. فرض سياسة الترخيص سيُفعل عند ربط خادم التفعيل الحقيقي حتى لا تُقفل نسخة التطوير بلا مفتاح إنتاجي.
- لم يتم بناء APK فعليًا أو تشغيل Flet UI على جهاز في هذه البيئة.
