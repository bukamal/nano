# طباعة عصرية + سعر الصرف التاريخي (الفواتير، السندات، كشف الحساب)

## الملخص

ثلاثة تعديلات مترابطة، كلها داخل نظام العملة المزدوجة + `DocumentService` (المستندات المطبوعة عبر مسار الطباعة/PDF الأصلي):

1. **تصميم عصري لكل المستندات المطبوعة**: الفاتورة، كشف الحساب، وشيت ملصقات الباركود.
2. **سعر صرف تاريخي لكل فاتورة**: الفاتورة تحفظ سعر الصرف والعملة المعروضة وقت إصدارها، وتطبعهما دائمًا — حتى لو غيّر المسؤول سعر الصرف الحالي لاحقًا من شاشة الإدارة.
3. **نفس الفكرة على السندات/الدفعات، وانعكاسها على كشف الحساب**: كل حركة في كشف حساب العميل/المورد تُطبع بسعر الصرف الذي كان ساريًا وقت حدوثها فعليًا (فاتورة أو سند)، بينما يبقى عمود "الرصيد" التراكمي دائمًا بسعر الصرف **الحالي** — لأنه يمثل المستحق الآن، لا رقمًا تاريخيًا.

## Schema Version 8 و 9

### تعديل جدول `invoices`

أضيفت 3 أعمدة جديدة (كلها Nullable):

- `invoice_exchange_rate`: سعر الدولار مقابل العملة المعروضة وقت إنشاء الفاتورة.
- `invoice_currency_code`: كود العملة المعروضة وقتها (`SYP`/`USD`).
- `invoice_currency_symbol`: رمز العملة المعروضة وقتها (مثل `ل.س` أو `$`).

القيم تُملأ مرة واحدة فقط عند `create_invoice`، ولا يعاد كتابتها عند `update_invoice` — نفس فلسفة `initial_paid_amount` في المرحلة 3: قيمة Source Data محفوظة عند الإنشاء، لا تتزحزح بتعديلات لاحقة.

### الترحيل من نسخ أقدم

عند فتح قاعدة بلا هذه الأعمدة:

1. تُضاف الأعمدة الثلاثة فارغة.
2. تُملأ فورًا من إعدادات `settings` **الحالية** (سعر الصرف/العملة/الرمز المعروضين الآن) لكل فاتورة قديمة — وهو نفس ما كانت تعرضه هذه الفواتير في كل مكان آخر من التطبيق قبل هذا التعديل، فلا يتغيّر شيء بصريًا لفاتورة قديمة إلا بعد أول تعديل لسعر الصرف من الآن فصاعدًا.
3. الفواتير الجديدة بعد هذا الإصدار تلتقط السعر الفعلي وقت الإنشاء، فتبقى دقيقة تاريخيًا مهما تغيّر سعر الصرف لاحقًا.

## أين يظهر السعر التاريخي؟

فقط عندما تكون عملة عرض الفاتورة وقتها SYP (لأن وضع USD معناه لا تحويل أصلًا). يظهر شريط صغير أعلى الفاتورة:

> سعر صرف الدولار وقت إصدار الفاتورة: **1 $ = 13,500 ل.س**

وكل المبالغ في تلك الفاتورة (الإجمالي، المدفوع، المتبقي، وأسطر الأصناف) تُحسب بهذا السعر المحفوظ، عبر `DocumentService._money(value, inv)` بدل الاعتماد المباشر على `settings` الحالية.

شيت الباركود ما زال يستخدم السعر **الحالي** كالمعتاد (لا سياق فاتورة واحدة له).

## Schema Version 9 — نفس الفكرة على `payments` (السندات)

### تعديل جدول `payments`

نفس النمط بالضبط: 3 أعمدة جديدة Nullable — `payment_exchange_rate`, `payment_currency_code`, `payment_currency_symbol` — تُملأ مرة واحدة عند إنشاء الحركة المالية ولا تتغيّر لاحقًا (`update_voucher` لا يكتب فوقها إطلاقًا، فتبقى القيمة الأصلية كما هي).

من أين تُملأ حسب مصدر الحركة:

- **`voucher`** (سند قبض/صرف مستقل من `PaymentService.create_voucher`): يلتقط إعدادات `settings` الحالية وقت إنشاء السند، تمامًا كما تفعل الفاتورة.
- **`invoice_initial`** (الدفعة الأولى المشتقة من الفاتورة نفسها، تُعاد توليدها تلقائيًا في كل `AccountingRebuilder.rebuild`): **ترث سعر الفاتورة الأم مباشرة** (`inv["invoice_exchange_rate"]`) بدل التقاط "الآن" — لأنها ليست حدثًا ماليًا مستقلًا، بل انعكاس للفاتورة، فمن الخطأ أن تحمل سعرًا مختلفًا عنها.
- **`legacy`** (حركات قديمة سابقة لهذا التعديل): تُملأ بالترحيل من إعدادات `settings` الحالية وقت فتح القاعدة، بنفس منطق ترحيل الفواتير.

### كشف الحساب: كل حركة بسعرها، والرصيد بالسعر الحالي

`StatementService.party_statement` أصبح يربط كل سطر في الكشف (`ledger_entries`) بمصدره الفعلي — فاتورة أو دفعة — ليجلب سعر الصرف الذي حدثت به تلك الحركة تحديدًا. `DocumentService.statement_html`:

- عمود **"الحركة"**: يُحسب بسعر الحركة نفسها وقت حدوثها (السعر التاريخي المحفوظ على الفاتورة أو السند المصدر).
- عمود **"الرصيد"** (التراكمي) وبطاقات الملخص (الافتتاحي/الختامي/الحالي): تبقى دائمًا بسعر الصرف **الحالي** — لأن الرصيد رقم حيّ يمثّل المستحق الآن بالضبط كما يظهر في كل مكان آخر بالتطبيق (لوحة التحكم، شاشة الطرف...)، وجمع مبالغ محوّلة بأسعار مختلفة تاريخيًا داخل رقم واحد لا معنى محاسبيًا له.
- عند وجود حركات بسعر تاريخي مختلف عن الحالي، تظهر ملاحظة توضيحية أعلى الجدول تشرح هذا الفرق للقارئ.

مثال حقيقي من الاختبار: فاتورة بـ100$ صدرت بسعر 13,500 (=1,350,000 ل.س) ثم تغيّر السعر إلى 20,000، ثم سُجّل سند قبض بـ40$. الكشف يعرض سطر الفاتورة بـ1,350,000 ل.س (سعرها وقتها) وسطر السند بـ800,000 ل.س (سعره وقته: 40×20,000)، بينما الرصيد بعد كل سطر محسوب بالكامل بسعر 20,000 الحالي.

## التصميم العصري

`_shell()` (الإطار المشترك لكل المستندات) أعيد بناؤه:

- ترويسة بخلفية متدرجة خفيفة بلون العلامة، مع شريط علوي سميك بنفس اللون، ورقم/عنوان المستند كـ badge بارز.
- بطاقات مقاييس (الإجمالي/المدفوع/المتبقي...) بحدّ جانبي ملوّن بدل الخلفية المسطحة فقط.
- badge ملوّن لحالة الفاتورة (مدفوعة/جزئية/غير مدفوعة) بثلاثة ألوان دلالية تبقى مقروءة عند الطباعة بالأبيض والأسود.
- جدول بترويسة ملونة بزوايا دائرية وصفوف متبادلة، ومحاذاة صحيحة للأرقام.
- شيت الباركود: بطاقات بحدود ناعمة وفاصل متقطع بين الاسم والباركود بدل الإطار المتقطع الكامل السابق، وسعر بلون العلامة التجارية.
- تذييل موحّد بسطرين (عربي/إنجليزي) في كل المستندات.

كل الألوان ما زالت مبنية على `invoice_color` من شاشة الإدارة (`AdminCenter`)، فأي عميل يخصص لونه الخاص يحصل على نفس الهوية في الفاتورة والملصقات وكشف الحساب دون أي كود إضافي.

## ولماذا لم يتأثر رصيد الأطراف نفسه؟

`customers.balance` / `suppliers.balance` / `ledger_entries.debit,credit` / `invoices.total,paid_amount` جميعها مخزّنة دائمًا بالدولار فقط (`STORED_CURRENCY_CODE = "USD"` في `currency.py`) — سعر الصرف طبقة عرض بحتة، لا تمس التخزين أو حساب `AccountingRebuilder` إطلاقًا. لذلك الرصيد المحاسبي الفعلي مطابق تمامًا قبل وبعد كل هذه التعديلات؛ ما تغيّر فقط هو **كيف يُعرض** كل رقم عند الطباعة.

## التوافق

- لا تغيير في توقيع أي دالة عامة (`invoice_html`, `statement_html`, `barcode_labels_html`) ولا في نقاط الاستدعاء في `invoice_view.py` / `pos_view.py` / `items_view.py` / `finance_view.py`.
- تم اختبار: إنشاء فاتورة → طباعتها → تغيير سعر الصرف من الإعدادات → إعادة طباعة نفس الفاتورة → التأكد أن السعر المطبوع لم يتغير. سند بعد الفاتورة بسعر مختلف → كشف الحساب يعرض كل سطر بسعره الخاص والرصيد بالسعر الحالي. تعديل فاتورة (`update_invoice`) بعد تغيير السعر → لا يغيّر سعرها التاريخي المحفوظ. وأيضًا ترحيل قاعدة قديمة كاملة (بدون أي من الأعمدة الجديدة على `invoices` و`payments`) والتحقق من التعبئة التلقائية وترقية `schema_version` إلى `9`، وأن الدفعة الأولية المُعاد توليدها من الفاتورة ترث سعرها لا سعر "الآن".
