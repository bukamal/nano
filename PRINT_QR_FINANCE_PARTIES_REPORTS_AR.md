# دعم الطباعة و QR للمالية والأطراف والتقارير

## الخلاصة
تمت إضافة إمكانية الطباعة الأصلية ومشاركة PDF (وQR كود حيثما كان مناسبًا) لثلاث
مناطق كانت تفتقر لها في النسخة السابقة، مع الحفاظ الكامل على نمط المشروع
الحالي (نفس `DocumentService._shell`، نفس أزرار "طباعة"/"مشاركة PDF" المستخدمة
في الفواتير وكشوف الحساب).

| المنطقة | الحالة قبل | الإضافة |
|---|---|---|
| المالية (كشف حساب طرف) | مطبّق مسبقًا في `finance_view.py` | لا تغيير — تم التأكد من التوافق فقط |
| **الأطراف** (`parties_view.py`) | لا يوجد طباعة إطلاقًا | زر طباعة + زر مشاركة PDF في تفاصيل كل عميل/مورد، مع **QR كبطاقة تواصل (vCard)** |
| **التقارير** (`reports_view.py`) | لا يوجد طباعة إطلاقًا لأي من التقارير الخمسة | زرا "طباعة التقرير" و"مشاركة PDF" يطبعان التقرير المعروض حاليًا بنفس الفلاتر (من/إلى تاريخ) |

## التفاصيل التقنية

### 1. `services/document_service.py`
- `party_card_html(party_type, data)`: بطاقة عميل/مورد قابلة للطباعة (الاسم،
  الهاتف، العنوان، الرصيد، عدد الفواتير، آخر الفواتير) + رمز QR بصيغة vCard
  قياسية (`BEGIN:VCARD...`) يمكن مسحه بأي تطبيق كاميرا عادي لإضافة جهة
  الاتصال مباشرة على أي هاتف — لا يتطلب تطبيق نانو نفسه على الجهاز الماسح،
  تمامًا كفكرة QR السعر في ملصقات الباركود.
- خمس دوال جديدة لعرض التقارير كمستند HTML قابل للطباعة:
  `pnl_report_html` (قائمة الدخل)، `profitability_report_html` (الربحية)،
  `inventory_report_html` (المخزون)، `balances_report_html` (الذمم)،
  `cash_report_html` (الصندوق). كل دالة تأخذ نفس البيانات التي تجلبها
  `ReportsCenter` أصلًا من `ReportingService` — بدون أي استعلامات إضافية
  لقاعدة البيانات، فقط عرض.

### 2. `views/reports_view.py`
- `ReportsCenter` أصبحت تقبل `native_files` (اختياري، بنفس نمط بقية الشاشات).
- زرا "طباعة التقرير" و"مشاركة PDF" يظهران دائمًا تحت شريط الفلاتر، ويعيدان
  بناء HTML التقرير الحالي (بنفس نوع التقرير والفترة المختارة) عبر
  `_report_html()` قبل الطباعة/المشاركة — فلا يمكن أبدًا أن تكون النسخة
  المطبوعة مختلفة عمّا هو معروض على الشاشة.

### 3. `views/parties_view.py`
- `PartyCenter` أصبحت تقبل `native_files`.
- في نافذة تفاصيل الحساب (Bottom Sheet)، أُضيف زرا أيقونة "طباعة" و"مشاركة"
  بجانب اسم الحساب، يستخدمان `document_service.party_card_html` بنفس بيانات
  `activity_summary` المعروضة أصلًا — بدون استعلام إضافي.

### 4. `main.py`
- تمرير `native_files` عند إنشاء `ReportsCenter` و `PartyCenter` (كانتا
  الوحيدتين من بين الشاشات التي تحتاج طباعة ولم تكونا تستقبلانه).

## ملاحظات توافق
- لا تغييرات على قاعدة البيانات أو الخدمات الأخرى — الإضافة بالكامل في طبقة
  العرض/التوليد.
- تم تحديث `tools/phase4_flet_ui_contract_smoke_test.py` ليطابق سطر إنشاء
  `ReportsCenter` الجديد.
- إن لم يكن `native_files` مهيأ (بناء غير أندرويد/iOS)، تظهر نفس رسالة
  "الطباعة الأصلية غير مهيأة في هذا البناء" المستخدمة في بقية الشاشات، بدل
  أي خطأ غير متوقع.
