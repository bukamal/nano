# تقرير التعديل: إصلاح رسم النمط، تأكيد استعادة النسخ، ودعم الباركود

## 1. إصلاح رسم النمط (`pattern_pad.py`) ✅
**السبب الحقيقي**: `_on_pan_start`/`_on_pan_update` كانا يقرآن `e.local_position.x`
و`e.local_position.y` من أحداث السحب (`DragStartEvent`/`DragUpdateEvent`). في
Flet 0.28.3 هذه الخاصية غير موجودة إطلاقًا — القيم الصحيحة هي `e.local_x` و
`e.local_y` مباشرة كأرقام عشرية مسطّحة. كل لمسة كانت تُطلق `AttributeError`
يُبتلع بصمت من مُرسل الأحداث في Flet، فلا يُرسم شيء ولا تظهر أي رسالة خطأ.

**الإصلاح**: استبدال القراءتين في كلا المعالجين بـ `(e.local_x, e.local_y)`.

## 2. إصلاح تأكيد استعادة النسخة الاحتياطية (`admin_view.py`) ✅
**السبب الحقيقي**: بعد نجاح `restore_backup()`، كان الكود يعرض `SnackBar` ثم
**فورًا** يستدعي `self.on_logout()` الذي يمسح `page.controls` بالكامل ويعيد
بناء شاشة الدخول — فتُحذف رسالة النجاح من الشجرة قبل أن يراها المستخدم فعليًا،
حتى لو استغرق ظهورها جزءًا من الثانية.

**الإصلاح**: استبدال الـ`SnackBar` بنافذة `AlertDialog` تأكيدية تعرض اسم نسخة
الأمان وتتطلب ضغط "متابعة" من المستخدم قبل استدعاء `on_logout()` — فيضمن رؤية
الرسالة دائمًا بغض النظر عن سرعة الانتقال.

## 3. دعم البيع بقراءة الباركود بالكاميرا ✅ (تم إنجازه بالكامل)

### قاعدة البيانات (`core/database.py`)
- `SCHEMA_VERSION` رُفع من 6 إلى 7.
- عمود `barcode TEXT` جديد في جدول `items`.
- فهرس فريد جزئي: `CREATE UNIQUE INDEX idx_items_barcode ON items(barcode)
  WHERE barcode IS NOT NULL AND TRIM(barcode) <> ''` — يسمح بعدد غير محدود من
  المواد بلا باركود (NULL أو فارغ) مع منع تكرار أي باركود فعلي.
- إضافة العمود تمت في `_pre_schema_migrate()` (قبل تنفيذ `SCHEMA_SQL`) لا في
  `_migrate()`، لأن إنشاء الفهرس الفريد أعلاه يفشل لو شغّلناه على قاعدة قديمة
  بلا العمود أولًا — نفس النمط المتبع مسبقًا لأعمدة phase-3.

### المستودع (`repositories/item_repository.py`)
- `create()`/`update()` يقبلان الآن `barcode: str | None`.
  السلسلة الفارغة تُطبَّع إلى `NULL` (لا تدخل تعارض الفهرس الفريد).
- `find_by_barcode(code)` جديدة — تُرجع المادة كاملة (نفس أعمدة `get()`) أو
  `None`.
- تكرار الباركود يُلتقط كـ `sqlite3.IntegrityError` ويُحوَّل إلى `ValueError`
  برسالة عربية واضحة ("هذا الباركود مستخدم بالفعل لمادة أخرى") بدل تسريب خطأ
  SQLite الخام للواجهة.

### الجسر الأصلي (Python ↔ Dart)
- `native_files.py`: دالة `scan_barcode()` جديدة — تستدعي `invoke_method_async`
  وتُرجع النص المقروء، أو `None` عند الإلغاء، أو ترفع `RuntimeError` عند خطأ
  حقيقي (نفس اتفاقية بقية دوال هذا الجسر).
- `native_files.dart`:
  - إضافة `case 'scan_barcode':` يفتح `_BarcodeScanPage` عبر
    `Navigator.push` مع `fullscreenDialog: true`، وينتظر النتيجة.
  - `_BarcodeScanPage` — صفحة كاملة الشاشة تستخدم حزمة `mobile_scanner`:
    فلاش قابل للتبديل، إطار توجيه بصري، وحماية `_handled` من استدعاء
    `onDetect` أكثر من مرة لنفس القراءة (سلوك معروف لبعض أجهزة أندرويد).
  - `pubspec.yaml`: إضافة `mobile_scanner: ^6.0.2`.
- `pyproject.toml`:
  - إذن `android.permission.CAMERA` في كل من `android_permissions` و
    `[tool.flet.android.permission]`.
  - `[tool.flet.android.feature]` لِـ `android.hardware.camera` و
    `android.hardware.camera.autofocus` بقيمة `false` (غير إلزامية) — حتى لا
    تُستبعد الأجهزة اللوحية أو أي جهاز بلا كاميرا من متجر التطبيقات.

### الواجهة — نموذج المادة (`views/items_view.py`)
- حقل "الباركود (اختياري)" + زر مسح (أيقونة كاميرا) بجانبه في نافذة
  إضافة/تعديل المادة.
- الزر يستدعي `native_files.scan_barcode()` ويملأ الحقل تلقائيًا عند النجاح.
- `ItemsCenter.__init__` يقبل الآن `native_files` (كان مفقودًا سابقًا) ويُمرَّر
  من `main.py` عند الإنشاء.
- `save()` يمرر `barcode=barcode.value or None` إلى `create`/`update`.

### الواجهة — شاشة فاتورة البيع (`views/invoice_view.py`)
- زر "مسح باركود" بجانب "إضافة بند".
- `scan_to_add_line()`: يمسح، يبحث بـ `ctx.items.find_by_barcode`، ثم:
  - إن كانت المادة موجودة مسبقًا كبند في نفس الفاتورة → يزيد كميته بمقدار 1
    (سلوك نقطة بيع طبيعي عند تكرار مسح نفس المنتج) بدل إضافة سطر مكرر.
  - وإلا → يضيف بندًا جديدًا بكمية 1.
  - لا توجد مادة بهذا الباركود → رسالة تنبيه بدل فشل صامت.
- **إصلاح جانبي ضروري**: `add_line(initial={...})` عند تمرير `item_id` فقط
  (بدون `unit_price`) كانت لا تملأ حقل السعر فعليًا (يبقى "0.00") رغم حساب
  `state["base_price"]` بشكل صحيح داخليًا — الحساب لم يكن يُكتب إلى حقل
  `price` نفسه إلا عبر `item_changed()` (مرتبط بـ `on_change`، لا يُستدعى عند
  البناء الأولي). تم سد هذه الفجوة بحساب السعر من `base_price × factor` في
  نفس مسار `initial.get("item_id")`، فتظهر البنود المُضافة بالمسح بسعر صحيح
  فورًا.

## التحقق الذي تم إجراؤه
- ✅ `python -m compileall` على `src`، `tools`، وامتداد
  `flet_native_files` — نجح بدون أخطاء.
- ✅ `PYTHONPATH=src python tools/quality_gate.py` كاملًا (**41 اختبارًا**،
  بما فيها الاختبار الجديد `barcode_scan_smoke_test.py`) — **كلها ناجحة**.
- ✅ اختبار يدوي إضافي (خارج quality_gate، أثناء التطوير) يحاكي قاعدة بيانات
  schema-6 حقيقية (بدون عمود `barcode` وبدون الفهرس) ويتحقق من: الترقية
  الآمنة، عدم فقدان بيانات السجلات القديمة، تعدد المواد بلا باركود، رفض
  التكرار، البحث، وتحرير الباركود عند مسحه من مادة لإعادة استخدامه في أخرى.
- ✅ فحص توازن الأقواس/البنية في `native_files.dart` (لا يمكن تشغيل Dart
  فعليًا في هذه البيئة).
- تحديث 3 اختبارات كانت تُثبّت `SCHEMA_VERSION == 6` حرفيًا إلى `== 7`:
  `phase7_service_cost_smoke_test.py` (موضعان)، `phase7_project_contract_
  smoke_test.py` (فحص نصي لسطر الكود)، و`quick_auth_saved_login_smoke_test.py`.

## قيد صادق (نفس قيد المشروع المعتاد)
- **جزء الكاميرا (Dart/Flutter) لم يُختبر على جهاز أو محاكي أندرويد حي** — لا
  تتوفر بيئة Flutter/Android هنا. تحققت من توازن الأقواس، صحة استدعاءات الـ
  API حسب توثيق حزمة `mobile_scanner` المعروف، وتناسق النمط مع بقية هذا
  الامتداد، لكن يُنصح بتجربة فعلية على جهاز قبل الإصدار — خصوصًا: طلب إذن
  الكاميرا وقت التشغيل الفعلي (لا وقت التثبيت)، وسلوك `MobileScannerController`
  عند إضاءة ضعيفة أو مسح سريع متكرر.
- `pyproject.toml` → `build_number` لم يُغيَّر عمدًا؛ يستحق قرارك بشأن ترقيمه
  مع هذه الميزة الجديدة قبل البناء الفعلي (`flet build apk`).
- لم أُضف `NSCameraUsageDescription` لأن هذا التطبيق يستهدف أندرويد فقط حاليًا
  (لا توجد إعدادات iOS في `pyproject.toml`) — إن أضفتم دعم iOS لاحقًا، هذا
  المفتاح مطلوب حينها في `[tool.flet.ios.info]`.
