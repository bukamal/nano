# Nano 0.7.4 — النقدي الافتراضي وPDF والنسخ الاحتياطية على Android

## التصحيحات

1. الفاتورة بلا عميل أو مورد تصبح نقدية ومسددّة بالكامل تلقائيًا داخل `InvoiceService`، وليس في الواجهة فقط.
2. PDF يُنشأ كملف فعلي في Cache ثم تتم مشاركته عبر `share_file` و`Share.shareXFiles`، نفس مسار مشاركة Backup.
3. استيراد Backup يستخدم `FileType.any` لتجاوز مشاكل امتدادات Android المخصصة، ثم يتحقق من ZIP/manifest/hash/schema قبل نسخه إلى مجلد النسخ المحلي.
4. إنشاء Backup أصبح باسم مؤرخ لتجنب الكتابة فوق نسخة سابقة، مع خيار **إنشاء ومشاركة نسخة** وخيار **إنشاء محليًا**.
5. النسخة المستوردة تُنسخ إلى تخزين Nano الدائم قبل فتح حوار الاسترجاع، فلا يعتمد الاسترجاع على Cache المؤقت لمنتقي الملفات.

- Version: 0.7.4
- Android build number: 11
