# تقرير: Swipe Actions — نُفّذت، بحدود أضيق من الأصل

## القرار الذي غيّر النطاق

قبل الكتابة، تحقّقت من طبقة البيانات: **المواد (`items`) لا تملك أي دالة
حذف على الإطلاق** — فقط `create`/`update`. أي "Swipe to delete" هناك كان
سيحتاج اختراع قدرة حذف غير موجودة أصلًا، وهذا تغيير في نموذج البيانات وليس
تحسين واجهة. لذلك **لم أطبّق Swipe Actions على المواد**.

العملاء/الموردون (`PartyCenter`, يخدم الشاشتين معًا) هم الحالة الوحيدة في
التطبيق التي تملك حذفًا حقيقيًا مع حارس فعلي من طبقة البيانات (`repo.delete`
يرفض إن وُجدت حركات مالية مرتبطة) ومربوطًا مسبقًا بحوار تأكيد. هذا هو
المرشح الآمن الوحيد — طبّقت عليه فقط.

## لماذا هذا آمن رغم نفس قيد المعاينة الحية (ينطبق عليّ أيضًا هنا)

الخطر الحقيقي في "فيزياء الإيماءة" ليس الشكل البصري — بل احتمال أن سحبة
سريعة (fling) تُنفّذ حذفًا فعليًا قبل أن يتمكن المستخدم من التراجع. في
Flutter، عنصر `Dismissible` قد يكتمل حذفه البصري بسحبة سريعة **حتى لو كانت
عتبة `dismiss_thresholds` مرتفعة** — أي لا يمكن الاعتماد على ضبط العتبة
وحدها لمنع حذف غير مقصود.

لذلك صممت التدفق بحيث **لا يعتمد أمان البيانات على ضبط الإيماءة إطلاقًا**:

- السحب (بأي من الاتجاهين) لا يحذف شيئًا مباشرة — فقط يفتح **نفس حوار
  التأكيد المركزي** (`confirm_delete`) المستخدم أصلًا من مسار الضغط
  (تفاصيل العميل ← حذف). لم أُنشئ مسار حذف جديدًا.
- ربطت الاتجاهين (يمين وشمال) **بنفس الفعل** بالضبط (نفس الخلفية الحمراء،
  نفس النص "حذف"، نفس الاستدعاء) — فلا داعٍ للتخمين أي اتجاه "صحيح" في
  واجهة RTL، لأن كلا الاتجاهين يؤديان لنفس النتيجة الآمنة.
- أضفت `restore_on_cancel` إلى `confirm_delete`: لأن `Dismissible` يُخفي
  الصف بصريًا بمجرد اكتمال السحبة (قبل أن يفتح الحوار)، فإن الإلغاء يجب أن
  **يُعيد** الصف. الإعادة تتم عبر `refresh()` نفسها — بما أن لا شيء حُذف
  فعليًا من قاعدة البيانات، فإعادة القراءة من `repo` تُعيد رسم العميل تلقائيًا
  بلا أي منطق يدوي لـ"استرجاع الصف".
- النتيجة: أسوأ سيناريو لسحبة خاطئة أو سريعة هو **حوار تأكيد غير مرغوب
  يُغلَق بلا أثر** — وليس حذف بيانات. هذا يطابق نفس مبدأ "الحوارات
  المدمّرة تبقى Modal" الذي التزمنا به سابقًا؛ Swipe هنا بديل بصري لمسار
  الوصول إلى نفس الـ Modal، لا بديل عن الـ Modal ذاته.

## قيد إضافي يخصّني أنا تحديدًا في هذه الجلسة

لا تتوفر حزمة `flet` هنا فعليًا (لا شبكة، لا نسخة مثبتة) — لذا استخدمت
`ft.Dismissible` / `ft.DismissDirection.HORIZONTAL` / `background` /
`secondary_background` / `on_dismiss` بناءً على معرفتي بواجهة Flet
الموثّقة لهذا العنصر، **دون قدرة على تشغيل `python -c "import flet"`
والتأكد من أسماء المعاملات حرفيًا مقابل `flet==0.28.3` المثبتة عندك**. هذا
قيد إضافي فوق قيد المعاينة الحية المعتاد.

**أول شيء يستحق تشغيله عندك قبل أي شيء آخر:**
```bash
python -c "import flet as ft; print(ft.Dismissible, ft.DismissDirection.HORIZONTAL)"
```
إن فشل هذا السطر بخطأ `AttributeError`، فالمشكلة في اسم معامل أو Enum فقط
(سهلة الإصلاح بمجرد رؤية الخطأ)، وليست في التصميم أو منطق الأمان أعلاه.

## التحقق الذي أنجزته فعليًا
- ✅ `ast.parse()` على `parties_view.py` — نجح.
- ✅ `python -m compileall src tools` (المشروع كاملًا) — نجح.
- ✅ `tools/core_smoke_test.py` — نفس النتائج المعتادة (لا يلمس طبقة الواجهة).
- ⚠️ لم يُشغَّل: أي استيراد فعلي لـ `flet` أو معاينة الشاشة.

## الملفات المعدّلة
```
src/nano_offline/views/parties_view.py   (تحديث - Swipe Actions للعملاء/الموردين + restore_on_cancel)
```

## لم يُنفَّذ عمدًا
- Swipe على المواد — لا يوجد حذف حقيقي يُسحب إليه (انظر أعلاه).
- Swipe على أي حوار آخر — لم يظهر مرشح ثانٍ بنفس معيار "حذف حقيقي + حارس
  من طبقة البيانات + حوار تأكيد موجود مسبقًا".
