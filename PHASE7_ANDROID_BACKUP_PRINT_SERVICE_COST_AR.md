# Phase 7 — Android Backup / Print / PDF / Service Cost

## الهدف

تحويل الوظائف التي بقيت مرتبطة ببيئة سطح المكتب إلى مسارات قابلة للاستخدام على Android، وإغلاق فجوة تكلفة الخدمات في تقارير الربحية، وتحسين تنقل الهاتف قبل اختبار APK فعلي.

## الإصدار

- التطبيق: `0.7.0`
- Build Number: `7`
- Flet: `0.28.3`
- SQLite Schema: `5`
- إضافة Flutter محلية: `flet-native-files 0.1.0`

## 1. جسر Android محلي للملفات والطباعة

أضيفت إضافة Flet محلية تحت:

`extensions/flet_native_files/`

وتوفر أربع عمليات فقط للواجهة:

- `pick_file()` لاختيار ملف من الجهاز.
- `share_file()` لمشاركة Backup أو أي ملف محلي.
- `print_html()` لطباعة مستند HTML.
- `share_pdf()` لتحويل HTML إلى PDF ومشاركته.

الجزء Python يظل مجرد Flet Control، بينما التنفيذ الفعلي للمنتقي والمشاركة والطباعة يتم داخل Flutter. تم تغليف ملفات Dart داخل Wheel، وأضيف اختبار يبني Wheel فعليًا ويتأكد من وجود الجزء Python والجزء Flutter معًا.

## 2. Backup على Android

شاشة الإدارة أصبحت تدعم:

1. إنشاء Backup محلي آمن كما في Phase 5.
2. مشاركة النسخة الناتجة عبر Share Sheet في Android.
3. اختيار ملف `.qeidbackup` من الجهاز.
4. التحقق من manifest والبصمة وSQLite قبل عرضه للاسترجاع.
5. إنشاء Safety Backup من القاعدة الحالية قبل الاستبدال.
6. ترحيل النسخة المسترجعة إلى Schema الحالي إذا كانت أقدم.
7. المحافظة على ترخيص الجهاز الحالي خارج النسخة المسترجعة.

لم يعد مسار Android يعتمد على كتابة التطبيق مباشرة إلى URI يعيده `FilePicker.save_file()`.

## 3. الفواتير وكشوف الحساب

أضيف `DocumentService` محلي يولد HTML عربي RTL قابل للطباعة والمشاركة.

### الفاتورة

تشمل:

- رقم الفاتورة ونوعها وتاريخها.
- العميل أو المورد.
- المرجع والملاحظات.
- البنود والوحدة والكمية والسعر والإجمالي.
- الإجمالي والمدفوع والمتبقي وحالة السداد.

ومن بطاقة الفاتورة أصبح هناك:

- `طباعة`
- `PDF`

### كشف الحساب

يعرض:

- اسم ونوع الحساب.
- الفترة.
- الرصيد الافتتاحي والختامي والحالي.
- الحركات المدينة والدائنة.
- الرصيد التراكمي.

ومنه يمكن تنفيذ:

- `طباعة الكشف`
- `مشاركة PDF`

كل البيانات تأتي من SQLite المحلية ولا توجد API للتقارير أو الطباعة.

## 4. تكلفة الخدمات

قبل Phase 7 كانت الخدمة لا تنتج حركة مخزون وبالتالي كان `cost_amount` في فاتورة البيع يساوي صفرًا؛ هذا كان يجعل ربح الخدمة مساويًا لسعر بيعها بالكامل.

أصبح `purchase_price` للخدمة يمثل **التكلفة المعيارية للخدمة**.

عند بيع الخدمة يحفظ بند الفاتورة Snapshot تاريخيًا:

- `unit_cost`
- `cost_amount`

مثال:

- تكلفة الخدمة الحالية: 30
- كمية البيع: 2
- سعر البيع الإجمالي: 200
- COGS: 60
- مجمل الربح: 140

إذا تغيرت تكلفة الخدمة لاحقًا إلى 50 فإن الفاتورة التاريخية تبقى COGS = 60. الفواتير الجديدة فقط تستخدم التكلفة الجديدة.

الخدمة لا تنشئ أي `inventory_movements` ولا تغير `stock_quantity`.

## 5. ترحيل Schema 4 → 5

أصبح Schema الحالي `5`.

عند ترقية قاعدة Phase 6:

- البنود القديمة للمواد تبقى كما هي.
- سطر خدمة بيع قديم بتكلفة صفرية يُعطى Snapshot مرة واحدة باستخدام تكلفة الخدمة المعرفة وقت الترحيل.
- بعد الترحيل لا تعاد كتابة تلك التكلفة التاريخية في عمليات إعادة بناء المخزون.

## 6. تحسين تنقل الهاتف

بدل وضع جميع الأقسام في Bottom Navigation على شاشة الهاتف، أصبحت الأقسام الأكثر استخدامًا هي الأساس:

- الرئيسية
- الفواتير
- المالية
- التقارير

وتظهر `المزيد` فقط عندما توجد صفحات ثانوية متاحة للمستخدم، وتحتها حسب الصلاحية:

- العملاء
- الموردون
- المواد والخدمات
- الإدارة

واجهة Desktop تستمر باستخدام NavigationRail الكامل.

## 7. Build Guard للإضافة

أضيف:

`tools/verify_flet_native_files_registration.py`

ويتم تشغيله في GitHub Actions بعد توليد مشروع Flutter. الفحص يتأكد من:

- وجود حزمة Flutter المحلية في `build/flutter-packages`.
- وجود ملفات Dart المطلوبة.
- وجود dependency في `pubspec.yaml` المولّد.
- وجود استيراد/تسجيل Control في bootstrap المولّد.

الغرض هو منع إنتاج APK يعمل فيه Python Control بينما Flutter لا يعرف `flet_native_files`.

## 8. تقوية ختم الترخيص المحلي

أثناء Regression Tests ظهر أن تعديل الحرف الأخير في Base64URL قد ينتج أحيانًا نفس البايتات بعد فك الترميز بسبب البتات غير المستخدمة في النهاية.

تم تقوية القراءة بحيث ترفض أي Base64URL غير canonical قبل قبول الختم. اختبارات Phase 6 للتلاعب بالترخيص عادت للنجاح.

## 9. الاختبارات

نجحت بوابة الجودة كاملة بعد Phase 7، ومنها:

- جميع اختبارات Phase 1–6 السابقة.
- `phase7_service_cost_smoke_test.py`
- `phase7_document_export_smoke_test.py`
- `phase7_native_extension_packaging_smoke_test.py`
- `phase7_ui_contract_smoke_test.py`
- `phase7_project_contract_smoke_test.py`
- `apk_release_preflight.py`

والنتيجة النهائية:

`quality_gate passed`

## 10. ما لم يُختبر فعليًا بعد

لم يتم تشغيل APK على جهاز Android داخل بيئة التنفيذ الحالية. لذلك ما زال يلزم اختبار Device QA لهذه المسارات:

- فتح Android file picker واستيراد Backup حقيقي.
- مشاركة Backup إلى تطبيق خارجي.
- فتح Print Service واختيار طابعة.
- مشاركة PDF إلى WhatsApp/Drive/Files.
- التثبيت والترقية مع الاحتفاظ بقاعدة SQLite والترخيص.
- اتجاه RTL وحجم العناصر على هاتف فعلي.

## القرار

Phase 7 تجعل المسار البرمجي جاهزًا لبناء APK واختبار Android الحقيقي. الخطوة التالية ليست إضافة Accounting Core جديد، بل تشغيل Release Build واختبار التثبيت والمشاركة والطباعة والاسترجاع على جهاز، ثم معالجة أي فروق Runtime قبل توسيع الوظائف.
