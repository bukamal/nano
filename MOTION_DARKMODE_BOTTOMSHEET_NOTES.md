# تقرير: حركة تنقّل + تأسيس وضع داكن + أول تحويل Bottom Sheet

استكمالًا لقائمة الأفكار المتقدمة، وبعد توضيح لك سبب عدم تنفيذ الأربعة دفعة
واحدة بنفس درجة الأمان — هذا ما نُفّذ فعليًا في هذه الخطوة، ولماذا بهذا الشكل
تحديدًا.

## 1) حركة التنقّل (Motion) — تم وأصبح فعّالًا

في `main.py`: حاوية المحتوى المشتركة (`content`) أصبحت
`animate_opacity=180`. دالة `navigate()` و`open_sale()`/`open_purchase()`
تُخفي المحتوى (`opacity = 0`) قبل استدعاء دالة الشاشة (التي تستبدل
`content.content` وتستدعي `page.update()` بنفسها أصلًا)، ثم تُعيد الإظهار
(`opacity = 1`) — فيظهر الانتقال كتلاشٍ ناعم بدل قفزة فجائية.

**لماذا هذا آمن رغم عدم القدرة على المعاينة الحية:** لا يوجد `asyncio.sleep`
أو توقيت مُدار يدويًا — هذا اعتماد كامل على الحركة الضمنية المدمجة في Flet
(`animate_opacity` على `Container`)، وهي خاصية قياسية موثّقة. أسوأ احتمال إن
كان هناك خطأ ما هو عدم ظهور حركة، وليس عطلًا.

## 2) تأسيس الوضع الداكن (`DarkColors` في `theme.py`) — أساس فقط، غير مُفعّل

أضفت كلاس `DarkColors` يحتوي **نفس الأسماء الـ35** الموجودة في `Colors`
حرفيًا (تحقّقت آليًا أن المجموعتين متطابقتان بالاسم) بقيم داكنة مناظرة.

**هذا ليس وضعًا داكنًا يعمل — وهذا مقصود، وليس نقصًا نسيته:**
كل شاشة في التطبيق تستدعي `Colors.X` مباشرة كسلسلة hex ثابتة (مئات
المواضع). تبديل `page.theme_mode` وحده **لن يُغيّر أي بطاقة أو نص** في
الواجهة — فقط عناصر Flet الأصلية غير المُخصَّصة (نادرة هنا). التفعيل
الحقيقي يتطلب تمرير كل تلك المواضع عبر دالة تُحلّ اللون حسب الوضع الحالي،
ثم **مراجعة بصرية لكل شاشة** للتأكد من التباين — خطوة لا يمكن إنجازها
بثقة بدون معاينة حية، فتركتها لجلسة مخصصة قادمة. ما أنجزته الآن هو
القرار الأصعب (تسمية كل رمز لوني وقيمته الداكنة) بحيث لا تبدأ تلك الجلسة
من الصفر.

## 3) أول تحويل فعلي: تفاصيل المادة من Dialog إلى Bottom Sheet

اخترت `show_item_detail` في `items_view.py` تحديدًا (وليس تحويلًا شاملًا
لكل الـ19 حوارًا) لأنه:
- حوار **معلوماتي بحت** (إحصاءات + حركات مخزون) بدون فعل تدميري — أفضل
  مرشح لتجربة السحب/الإغلاق الطبيعية لـ Bottom Sheet.
- استخدم نفس نمط `ft.BottomSheet` + مقبض السحب + الزوايا العلوية الدائرة +
  `Shadow.LG` **الموجود مسبقًا ويعمل فعليًا** في شيت "المزيد" بـ `main.py` —
  لم أخترع نمطًا جديدًا غير مُختبر.
- المنطق (الإحصاءات، حركات المخزون، الوحدات) لم يتغيّر إطلاقًا — فقط الغلاف
  البصري (Dialog → BottomSheet) وتحويل أزرار `dialog.actions` إلى صف أزرار
  عريضة أسفل المحتوى.

**حوارات التأكيد/الحذف الـ18 الأخرى تُركت كما هي عمدًا** — الـ Modal
المركزي هو النمط الصحيح لفعل يحتاج انتباهًا كاملًا قبل تأكيده (حذف، دفع)،
وتحويلها لشيت سيكون تراجعًا في الوضوح لا تحسينًا.

## التحقق
- ✅ `ast.parse()` على `theme.py`, `main.py`, `items_view.py` — نجح.
- ✅ فحص آلي: أسماء الرموز في `DarkColors` مطابقة 100% لـ `Colors` (35/35).
- ✅ `python -m compileall src tools` — نجح.
- ✅ `tools/core_smoke_test.py` — نفس النتائج المعتادة.
- ⚠️ **القيد المعتاد**: لا معاينة Flet حية. راجع خصوصًا: سلاسة التلاشي بين
  الشاشات، وشكل شيت تفاصيل المادة (خصوصًا على ارتفاع شاشة صغير).

## الملفات المعدّلة
```
src/nano_offline/core/theme.py       (تحديث - كلاس DarkColors جديد، غير مُفعّل)
src/main.py                           (تحديث - حركة التنقّل)
src/nano_offline/views/items_view.py (تحديث - تفاصيل المادة إلى Bottom Sheet)
```

## المتبقي من القائمة الأصلية (ما زال يحتاج جلسة مخصصة بمعاينة حية)
- **تفعيل** الوضع الداكن فعليًا (تمرير كل الاستخدامات عبر `DarkColors` + مراجعة تباين كل شاشة).
- **Swipe Actions** على صفوف القوائم — يحتاج ضبط فيزياء إيماءة حي، لا يمكن تخمينه بأمان.
- تحويل بقية حوارات المعلومات (وليس التأكيد) لـ Bottom Sheet، بنفس المعيار الانتقائي أعلاه، لو رغبت.
