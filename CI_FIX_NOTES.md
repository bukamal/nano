# تقرير: إصلاح 3 اختبارات كسرها تقسيم main.py — تم اكتشافها عبر CI الفعلي عندك

شكرًا لتشغيل `quality_gate.py` الفعلي — هذا بالضبط ما كنت أطلب التحقق منه لأنني
لم أستطع تشغيل Flet في بيئتي. النتيجة: **3 اختبارات فشلت**، والسبب في الثلاثة
واحد ومتوقع تمامًا: اختبارات "regression contract" تتحقق من **نص حرفي داخل
`main.py`** كدليل على أن سلوكًا معينًا لا يزال موجودًا — وبعد نقل الأقسام
الأربعة إلى ملفات Center منفصلة (الجلسة السابقة)، تحرّك ذلك النص الحرفي إلى
ملف آخر، فرسب الاختبار رغم أن **السلوك الفعلي نفسه لم يتغيّر إطلاقًا**.

## الاختبارات الثلاثة وسبب كل فشل

### 1) `invoice_ui_android_regression_smoke_test.py`
كان يتحقق أن `main.py` يحتوي `set_header("المواد"` و`"التصنيفات والوحدات"`
(جزء من إصلاح تاريخي لعطل "الشاشة الرمادية على أندرويد"). كلا النصين انتقل إلى
`ItemsCenter` في `items_view.py` (بصيغة `self._set_header("المواد"...)`).
**الإصلاح:** حدّثت الاختبار ليقرأ `items_view.py` بدل `main.py` لهذين السطرين
تحديدًا، مع تعليق يوثّق سبب النقل.

### 2) `qeid_reference_design_phase1_smoke_test.py`
3 نصوص (`"إجراءات سريعة"`, `"آخر الفواتير"`, `"تنبيهات"`) كانت جزءًا من محتوى
لوحة التحكم، وانتقلت إلى `DashboardCenter` في `dashboard_view.py`.
**الإصلاح:** نفس الأسلوب — الاختبار الآن يقرأ `dashboard_view.py` لهذه الثلاثة
تحديدًا، والباقي (`sale_fab`, `mobile_item(...)`, إلخ) بقي يُفحص في `main.py`
لأنه لم يتحرك فعليًا (هذا جزء الشل/التنقل).

### 3) `qeid_reference_design_phase2_smoke_test.py`
5 نصوص توزّعت على 3 ملفات بعد النقل:
- `"مخزون منخفض"`, `"آخر حركات المخزون"`, `"كمية مباعة"` → `items_view.py`
- `"إجمالي الفواتير"` → `parties_view.py`
- `"آخر الفواتير"` → `dashboard_view.py`
**الإصلاح:** نفس الأسلوب، كل نص يُفحص في مكانه الجديد الصحيح.

## لماذا هذا إصلاح صحيح وليس "تمرير الاختبار قسرًا"
في الحالات الثلاث لم أُضعف أو أحذف أي تحقق — فقط غيّرت **أي ملف** يُقرأ لكل
سطر بعينه، بما يطابق مكانه الحقيقي الجديد بعد إعادة الهيكلة. كل نص لا يزال
يجب أن يكون موجودًا **حرفيًا** بنفس الصيغة، وكل الاختبارات المنطقية (الحسابية،
قاعدة البيانات، الترخيص، النسخ الاحتياطي...) لم تُمس إطلاقًا لأنها لا علاقة
لها بمكان الكود.

## التحقق النهائي
```
$ PYTHONPATH=src python tools/quality_gate.py
...
quality_gate passed
```
**كل الاختبارات الـ~40 نجحت**، بما فيها الستة التي لم تصل إليها CI عندك أصلًا
بسبب توقف التنفيذ عند أول فشل (`search_select_contract_smoke_test`,
`quick_auth_saved_login_smoke_test`, `cash_backup_pdf_android_smoke_test`,
`qeid_reference_design_phase1/2`, `apk_release_preflight`) — شغّلتها كلها هنا
يدويًا بعد الإصلاح للتأكد الكامل.

## الملفات المعدّلة
```
tools/invoice_ui_android_regression_smoke_test.py   (تحديث)
tools/qeid_reference_design_phase1_smoke_test.py    (تحديث)
tools/qeid_reference_design_phase2_smoke_test.py    (تحديث)
```

## درس للمستقبل
هذا النمط من الاختبارات (regression contracts تقرأ نص الكود المصدري حرفيًا)
هش أمام أي إعادة هيكلة مستقبلية، حتى لو لم تُغيّر أي سلوك. إن كنت تخطط لمزيد
من إعادة الهيكلة لاحقًا، يستحق التفكير في تحويلها لاحقًا لاختبارات سلوكية
حقيقية (تُشغّل الكود وتتحقق من النتيجة) بدل فحص نص الملف — لكن هذا خارج نطاق
الإصلاح الحالي ولم ألمسه.
