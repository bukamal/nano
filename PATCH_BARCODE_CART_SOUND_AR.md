# تقرير: إصلاح اختفاء السلة نهائيًا + صوت فشل المسح في الجرد المستمر

## 1. السلة تختفي نهائيًا بعد أول مسح باركود (`pos_view.py`)

`barcode_field.on_focus`/`on_blur` يُخفيان/يُظهران `cart_wrap` (لإفساح
المجال للوحة المفاتيح عند الكتابة). لكن `barcode_submit` كانت تنادي
`barcode_field.focus()` في نهايتها لإبقاء التركيز على الحقل لالتقاط
مسحة الباركود التالية مباشرة (ماسح خارجي/متصل بالبلوتوث يكتب الرمز ثم
Enter) — وبما أن الحقل **لم يفقد التركيز فعليًا** بين مسحة وأخرى،
حدث `on_blur` لا يُطلَق إطلاقًا بعد أول مسحة، فتبقى السلة مخفية إلى
الأبد حتى لو أغلق المستخدم لوحة المفاتيح يدويًا.

**الإصلاح:** `barcode_submit` تُعيد إظهار `cart_wrap.visible = True`
صراحة في كل مرة تُعالَج فيها مسحة، فلم تعد رؤية السلة متعلقة بحدث
`blur` قد لا يحدث أبدًا مع ماسح مستمر.

## 2. صوت الفشل/التحذير في الجرد المستمر لم يكن يعمل إطلاقًا (`stocktake_view.py`)

دالة `show_last_scan` كانت تستدعي `self.notify(..., sound_kind="scan")`
فقط عند النجاح (`status == "added"`) — أما حالات: باركود غير موجود
(`else`)، مادة خدمة (`service`)، رقم تحقق خاطئ (`checksum`)، ومطابقة
غير تامة (`similar`) فكانت تكتفي بتحديث بطاقة `last_scan_card` المرئية
مباشرة دون أي نداء لـ `notify()`/`toast()` — وبما أن نظام الصوت بأكمله
مبني على المرور عبر `toast()` (انظر تعليق `core/sound.py`)، فهذه
الحالات الأربع لم تُصدر أي صوت مطلقًا، بصرف النظر عن إعدادات الصوت.

**الإصلاح:** أُضيف نداء `self.notify(...)` مناسب لكل حالة (`error` للباركود
غير الموجود، `warning` للحالات الثلاث الأخرى)، مضبوطًا خلف نفس مفتاح
`stocktake_sound_enabled` المستخدم أصلاً لحالة النجاح.

## لم يُختبر على جهاز حقيقي

نفس القيد المذكور في `SOUND_BARCODE_DELETE_FIX_AR.md`: لا توجد بيئة
Flutter/جهاز Android هنا. تم التحقق فقط عبر `py_compile` للملفين
المعدَّلين وتشغيل `tools/sound_system_smoke_test.py` و
`tools/stocktake_continuous_scan_smoke_test.py` (كلاهما ناجح).

**تنبيه مهم بخصوص الصوت في نقطة البيع تحديدًا:** راجعتُ مسار الصوت
كاملاً في `pos_view.py`/`core/sound.py`/`core/toast.py`/امتداد
`flet_native_files` (Python وDart) ولم أجد فيه أي خلل برمجي ظاهر —
الربط يبدو صحيحًا نظريًا. إن استمر غياب الصوت هناك تحديدًا بعد هذا
التحديث، فالسبب الأرجح ليس في الكود بل أن **التطبيق المثبَّت على
الجهاز لم يُعَد بناؤه فعليًا** بعد آخر جلسة أضافت أصوات WAV الجديدة —
هذه تغييرات على الجانب الأصلي (Flutter/Dart)، تحتاج `flet build apk`
كاملة وإعادة تثبيت الـ APK، لا تكفي فيها إعادة تشغيل بايثون وحدها. يُنصح
بالتحقق من: (أ) تبويب الإدارة ← "الصوت" — المفتاح العام ومفتاح "خطأ"
مفعّلان ومستوى الصوت أعلى من صفر، (ب) زر "تشخيص المشكلة" في نفس التبويب
لمعرفة إن كانت الملفات الصوتية محمّلة فعليًا على الجهاز، (ج) إعادة بناء
وتثبيت الـ APK من جديد.
