# Nano Installer Output

When building with `build_nano_apk.sh` or `build_nano_apk.bat`, the expected installer output is:

- `build/apk/release/nano-release.apk`

This repository includes the Nano branding assets and Flet packaging files needed for the installer icon and splash screens.
