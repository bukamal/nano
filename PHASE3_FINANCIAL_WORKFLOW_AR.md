# المرحلة 3 — السندات والدفعات والمصروفات وكشوف الحساب

## الهدف

إغلاق الحلقة المالية بعد الفاتورة في نسخة `qeid-offline`: تسجيل قبض العميل وصرف المورد بعد إنشاء الفاتورة، توزيع الدفعات، دعم الرصيد الدائن، المصروفات، وكشف الحساب، مع بقاء جميع البيانات محلية في SQLite وعدم وجود أي اعتماد محاسبي على الإنترنت.

## Schema Version 3

أصبح إصدار قاعدة البيانات `3`.

### الجداول الجديدة

- `vouchers`: مستند سند القبض/الصرف.
- `payment_allocations`: توزيع حركة مالية واحدة على فاتورة واحدة أو عدة فواتير.
- `expense_categories`: تصنيفات المصروفات المحلية.

### تعديلات الجداول

#### invoices

أضيف:

- `initial_paid_amount`: الدفعة التي أُدخلت عند إنشاء/تعديل الفاتورة، وهي Source Data.
- `paid_amount`: أصبح قيمة مشتقة من مجموع `payment_allocations` المرتبطة بالفاتورة.

هذا الفصل يمنع تعديل الدفعة الأولى من أن يمحو دفعات لاحقة على الفاتورة.

#### payments

أضيف:

- `reference`
- `source_type`
- `source_id`

الحركة المالية قد تأتي من:

- `invoice_initial`: دفعة أولى مولدة من الفاتورة.
- `voucher`: سند قبض/صرف مستقل.
- `legacy`: حركة مستقلة مرحّلة من إصدار سابق إن وجدت.

#### expenses

أضيف:

- `category_id`
- `reference`
- `notes`
- `updated_at`

وتُرحّل قيمة `category` النصية القديمة إلى `expense_categories` دون حذف النص القديم.

## سندات القبض والصرف

### سند قبض

يرتبط بعميل ويولد:

- Debit على `CASH`.
- Credit على `AR` للعميل.

### سند صرف

يرتبط بمورد ويولد:

- Debit على `AP` للمورد.
- Credit على `CASH`.

يمكن إنشاء وتعديل وحذف السند. الحذف يحذف توزيعاته ثم يعيد بناء رصيد الطرف والمدفوع على الفواتير.

## توزيع الدفعة

يدعم النظام ثلاث طرق:

1. **الأقدم أولًا**: يوزع المبلغ على أقدم الفواتير المفتوحة للطرف حتى نفاد المبلغ.
2. **يدوي**: يحدد المستخدم مبلغًا لعدة فواتير، مع منع تجاوز المتبقي لكل فاتورة أو قيمة السند.
3. **دون توزيع**: تبقى الحركة رصيدًا دائنًا/مقدمًا على حساب الطرف.

إذا كان مبلغ السند أكبر من مجموع الفواتير المفتوحة، الجزء الزائد لا يضيع ولا يُجبر على فاتورة؛ يبقى رصيدًا على الحساب.

## الأرصدة الدائنة

اصطلاح الرصيد الداخلي:

- العميل موجب: مبلغ مستحق لنا.
- العميل سالب: رصيد دائن للعميل/مبلغ مدفوع مقدمًا.
- المورد موجب: مبلغ مستحق للمورد.
- المورد سالب: دفعة مقدمة للمورد.

لوحة التحكم تفصل الآن بين:

- ذمم العملاء الموجبة.
- الأرصدة الدائنة للعملاء.
- ذمم الموردين الموجبة.
- الدفعات المقدمة للموردين.

## دفعة لاحقة من شاشة الفاتورة

عندما تكون الفاتورة غير مسددة ولها عميل/مورد، يظهر زر `تسجيل دفعة`.

الزر لا يعدل `invoices.paid_amount` مباشرة. ينشئ سندًا ماليًا ويخصصه للفواتورة، ثم يعاد حساب المدفوع والمتبقي والذمة.

## تعديل/حذف الفواتير مع وجود دفعات

- الدفعات الأولى يعاد توليدها من `initial_paid_amount`.
- توزيعات السندات المستقلة تبقى محفوظة.
- حذف فاتورة يحذف تخصيصات السند إليها فقط، بينما يبقى السند نفسه رصيدًا على حساب الطرف.
- إذا تم تخفيض إجمالي فاتورة إلى أقل من الدفعات الموزعة عليها، تفشل العملية كاملة ويتم Rollback بدل إنتاج فاتورة بمدفوع أكبر من الإجمالي.
- تغيير طرف فاتورة عليها توزيع من سند طرف آخر يُرفض بواسطة التحقق من عقد التوزيع.

## المصروفات

تم تنفيذ:

- تصنيفات مصروفات محلية.
- إنشاء/تعديل/حذف مصروف.
- التاريخ، البيان، القيمة، المرجع والملاحظات.
- قيد `EXPENSE` مدين و`CASH` دائن.
- إعادة بناء قيود المصروفات القديمة عند فتح قاعدة Phase 2.

## كشف حساب العميل والمورد

`StatementService` يعتمد على `ledger_entries` وليس على تجميع واجهة منفصل.

كشف العميل يستخدم حساب `AR`:

- الفاتورة تزيد الرصيد.
- القبض يخفضه.

كشف المورد يستخدم `AP`:

- فاتورة الشراء تزيد الالتزام.
- الصرف يخفضه.

الكشف يعرض:

- الرصيد الافتتاحي للفترة.
- الحركات بترتيب التاريخ.
- مقدار أثر الحركة على الرصيد.
- الرصيد التراكمي.
- الرصيد الحالي.
- الفواتير المفتوحة.
- الرصيد الدائن إن وجد.

## واجهة Flet

أضيف قسم `المالية` إلى التنقل الرئيسي ويحتوي على:

- السندات.
- المصروفات.
- كشف العملاء.
- كشف الموردين.

واجهة السند تدعم طرق التوزيع الثلاث. التوزيع اليدوي يعرض الفواتير القابلة للتخصيص والمبلغ المتاح لكل منها.

## الترحيل من Phase 2

عند فتح قاعدة Schema 2:

1. تضاف `initial_paid_amount` وتنسخ إليها قيمة `paid_amount` القديمة.
2. تزال صفوف الدفعات المشتقة القديمة المرتبطة بالفواتير فقط.
3. تنشأ دفعات أولية جديدة من `initial_paid_amount`.
4. تنشأ `payment_allocations` المقابلة.
5. يعاد حساب `paid_amount` وأرصدة العملاء والموردين والـLedger.
6. تصنيفات المصروفات النصية القديمة تحول إلى `expense_categories`.
7. تعاد قيود المصروفات القديمة.

## اختبارات المرحلة

يغطي `phase3_financial_workflow_smoke_test.py`:

- فاتورة بيع بدفعة أولى.
- سند قبض لاحق.
- توزيع الأقدم أولًا.
- توزيع يدوي على أكثر من فاتورة.
- مبلغ زائد يتحول إلى رصيد دائن.
- حذف سند وعكس أثره.
- تسجيل دفعة مباشرة من الفاتورة.
- فاتورة شراء وسند صرف.
- دفعة مقدمة للمورد.
- كشف حساب العميل والمورد.
- إنشاء/تعديل/حذف مصروف.
- رفض تخفيض فاتورة تحت قيمة التوزيعات الحالية مع Rollback.

ويغطي `phase3_schema_migration_smoke_test.py` ترحيل قاعدة تحاكي Phase 2 إلى Schema 3 مع الحفاظ على الدفعة الأولى وتصنيف المصروف وإعادة بناء القيود.

## ما بقي للمراحل التالية

- مركز التقارير التفصيلي وربحية المواد/الفترات.
- تقارير المخزون والحركة وإعادة الطلب.
- طباعة/PDF للفواتير والسندات والكشوف.
- Backup/Restore مع فحص سلامة SQLite.
- المستخدمون المحليون والصلاحيات وسجل تدقيق موسع.
- نظام التفعيل الأونلاين والتوقيع الرقمي.
- بناء APK واختبار UI فعلي على Android.
