# إصلاح مشاركة قاعدة البيانات بين التطبيقات الثلاثة

تاريخ: 2026-09-12

## المشاكل التي عُالجت

1. **خطأ «التطبيق غير مثبت»**  
   - authority فريد لكل APK: `${applicationId}.shared.db`  
   - عدم تعريف إذن مخصص مكرر بين الحزم  

2. **عدم قراءة قاعدة البيانات من التطبيقات الأخرى**  
   - على Android 11+ بدون صلاحية All files access يسقط المسار إلى مجلد خاص بكل تطبيق  
   - النتيجة: كل APK يرى قاعدة بيانات مختلفة/فارغة  

## الملفات المعدّلة

- `extensions/.../NanoSharedStorage.kt` — تفضيل المسار العام + تحذير واضح عند السقوط للخاص
- `src/nano_offline/shared_storage_boot.py` — كشف `needs_permission` + تسجيل السبب
- `src/nano_offline/bootstrap.py` — تحذير عند اكتشاف أن القاعدة غير مشتركة
- `apps/README_MULTI_APP_AR.md` — توثيق خطوات منح الصلاحية

## مطلوب على الجهاز

1. منح **الوصول إلى كل الملفات** لكل من: المحاسبة، المستودع، نقطة البيع  
2. إعادة تشغيل التطبيقات بعد منح الصلاحية  
3. التحقق أن المسار أصبح:  
   `/storage/emulated/0/Documents/NanoShared/nano.db`  
   وأن `truly_shared = true`
