# Nano 0.8.5 — تصحيح لوحة التشخيص + الدليل الفعلي على موضع العطل

## الخطأ في لوحة التشخيص نفسها (0.8.4)

اللوحة كانت تعرض "نجحت بلا استثناء" لصفَّي قراءة البيانات ورسم المحتوى
اعتمادًا فقط على أن `last_state_read_error`/`last_render_error` قيمتهما
`null` — لكن القيمة الابتدائية لهما `null` أيضًا قبل أي تنفيذ إطلاقًا. أي
أن "لم يحدث بعد" و"حدث ونجح" كانا يظهران بنفس العلامة الخضراء ✓. صورة
الشاشة التي أرسلتها كشفت هذا التناقض بوضوح: "آخر مرة أُعيد فيها بناء محتوى
الودجت: **لم يحدث بعد**" (رمادي) بينما نفس الشاشة تعرض صفَّي القراءة
والرسم بعلامة ✓ خضراء — تناقض منطقي مباشر.

التصحيح: الصفّان الآن يُقيَّدان بـ`last_provide_glance_at`. إن كان صفرًا
(لم يُنفَّذ `provideGlance` إطلاقًا) يظهر السطر بعلامة محايدة "لم يحدث بعد"
بدل ✓ الكاذبة.

## ما كشفته صورتك فعليًا — وهو الأهم

بعد التصحيح، مجموعة الحقائق في صورتك تعني حرفيًا:

- الودجت مضافة فعليًا للشاشة الرئيسية (`widget_count = 1`).
- التحديث الفوري (push) نجح ووصلت له بيانات فعلية (رغم كونها أرقامًا غريبة
  الشكل — `0.074074...`/`-222.109...` — ملاحظة جانبية منفصلة عن الودجت، على
  الأرجح خطأ حسابي في `DashboardService`، ليست موضوع هذا الإصلاح).
- **لكن `provideGlance` — الدالة الوحيدة التي ترسم محتوى الودجت فعليًا —
  لم تُستدعَ ولو مرة واحدة.**

هذا يعني أن كل الحمايات المضافة في 0.8.3 و0.8.4 (try/catch حول قراءة
DataStore وحول الرسم) **لا يمكنها أن تكون سبب** "يتعذّر عرض المحتوى" التي
لا تزال تظهر، لأن كودها لم يُنفَّذ إطلاقًا. العطل الفعلي يقع في طبقة أعلى:
داخل آلية جلسة Glance نفسها (`GlanceAppWidgetReceiver` → `AppWidgetSession`
→ استدعاء `provideGlance`) — وهذه طبقة من مكتبة `androidx.glance` نفسها،
لا كود من هذا المشروع، وبالتالي لا تصلها أي try/catch كتبناه حتى الآن.

## لماذا لا يمكن تشخيصها أكثر من الكود وحده

كل تشخيص حتى الآن (0.8.2 إلى 0.8.5) اعتمد على قراءة الكود ومطابقة الأعراض.
هذه هي نقطة النهاية لهذا الأسلوب: لا مزيد من الأدلة يمكن استخلاصها من الكود
المصدري وحده لمعرفة *لماذا* لا تستدعي Glance دالة `provideGlance` إطلاقًا.
الاحتمالات المفتوحة (بلا ترجيح واحد منها بلا دليل):

- فشل صامت داخل خدمة Glance الداخلية (`GlanceRemoteViewsService` أو ما
  يعادلها) لم يُسجَّل في أي مكان يصل إليه كود هذا المشروع.
- تعارض إصدارات بين `androidx.glance`/`androidx.compose.ui` في وحدة
  الإضافة وأي نسخة أخرى تجلبها Flet في التطبيق الرئيسي وقت التشغيل (لا
  وقت الترجمة).
- استثناء غير ملتقط في خيط/coroutine داخلي لمكتبة Glance نفسها.

## الخطوة الوحيدة المتبقية

التقاط `adb logcat` من الجهاز الفعلي في اللحظة التي تظهر فيها "يتعذّر عرض
المحتوى" (أو حتى عند مجرد إضافة الودجت من جديد):

```
adb logcat | grep -iE "glance|homewidget|AppWidget|nano"
```

نص الاستثناء الفعلي في هذا السجلّ هو المعلومة الوحيدة القادرة على تضييق
السبب أكثر من هذا. الصق ناتج هذا الأمر وسأحلله مباشرة.

- Version: 0.8.5
- Android build number: 17
